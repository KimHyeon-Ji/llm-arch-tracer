# 파생 차원 규칙 — config 필드 하나로 안 떨어지고 **계산**으로 나오는 고정 차원의 이름표.
#
# 왜 필요한가: symbols.yaml은 "config 필드 ↔ 심볼" 1:1 매핑만 한다. 하지만 실제 트레이스에는
# `n_h·d_head/g_o`(그룹 출력 투영 폭), `T + T/m`(압축 KV 길이) 같은 **합성 차원**이 잔뜩 나온다.
# 이게 등록되어 있지 않으면 model_summary.md의 "유도 상수" 표에 정체불명 숫자로 남는다.
#
# 규칙(01-main.md P1): 여기 있는 건 전부 **계산해서 값이 정확히 일치할 때만** 붙는 이름이다.
# 인수분해로 아무 이름이나 갖다 붙이지 않는다. 새 아키텍처를 만나 정체불명 숫자가 남으면
# 02-new-module-handling.md의 "신규 모듈 조사 절차"를 따라 **믿을 만한 소스로 확인한 뒤**
# 여기에 식과 출처를 함께 추가한다. 확인 못 하면 추측해서 넣지 말고 미해결로 남겨
# 사람 검증(Tier 3)으로 넘긴다.
#
# vars: 심볼이 아닌 부수 스칼라(중첩 dict 값, float 계수 등)를 eval 네임스페이스에 올린다.
#   from.aliases  config 속성명 후보
#   from.field + from.key  dict 필드에서 key로 꺼냄
# rules: expr를 심볼+vars로 평가해 값이 맞으면 name을 붙인다. 사용 가능한 함수: ceil, round, min, max.

vars:
  m_csa:
    meaning: "CSA 압축률 m (토큰 m개 → 압축 엔트리 1개)"
    from: {field: compress_rates, key: compressed_sparse_attention}
  m_hca:
    meaning: "HCA 압축률 m′"
    from: {field: compress_rates, key: heavily_compressed_attention}
  pr:
    meaning: "부분 RoPE 비율 (회전 차원 / head 차원)"
    from: {aliases: [partial_rotary_factor]}
  ffn_f:
    meaning: "FFN 확장 계수 (xLSTM 계열)"
    from: {aliases: [ffn_proj_factor]}
  ffn_r:
    meaning: "FFN 차원 반올림 단위"
    from: {aliases: [ffn_round_up_to_multiple_of]}
  qk_f:
    meaning: "xLSTM q/k 투영 폭 계수 (d_model 대비)"
    from: {aliases: [qk_dim_factor]}
  v_f:
    meaning: "xLSTM v 투영 폭 계수 (d_model 대비)"
    from: {aliases: [v_dim_factor]}

rules:
  # ---- MLA q/k head 폭 ----
  # DeepSeek-V3는 config.__init__ 에서 qk_head_dim = qk_nope_head_dim + qk_rope_head_dim 을
  # 미리 계산해 필드로 들고 있어서, 모델 코드가 그 합을 다시 계산하지 않는다. 그래서 이 값만
  # 이름을 못 받고 정수로 남았다(V2-Lite는 inline 계산이라 자동으로 붙는다).
  # 출처: transformers/models/deepseek_v3/configuration_deepseek_v3.py:122 (실행 중인 코드).
  - expr: "d_nope + d_rope"
    scope: "attn|attention|compressor|indexer"
    sym: "d_nope+d_rope"
    name: "d_nope + d_rope (MLA q/k head 폭)"

  # ---- 부분 RoPE (partial rotary) ----
  # 회전은 head 차원의 앞 d_rope에만 걸고 나머지는 그대로 통과 후 다시 concat.
  # 출처: transformers deepseek_v4 / qwen3_next modeling의 apply_rotary_pos_emb 분할.
  - expr: "d_head - d_rope"
    scope: "attn|attention|rotary|compressor|indexer"
    sym: "d_head-d_rope"
    name: "d_head − d_rope (부분 RoPE 비회전 통과분)"
  - expr: "d_head - round(d_head * pr)"
    scope: "attn|attention|rotary|compressor|indexer"
    sym: "d_head-d_rope"
    name: "d_head − d_rope (부분 RoPE 비회전 통과분, partial_rotary_factor 기준)"

  # ---- Lightning Indexer 자체 폭 (③ 모듈-필드 소속 검사 2026-08-11) ----
  # indexer 는 self_attn 안에 있지만 자기 head 수·head 폭(index_n_heads / index_head_dim)으로
  # 지어진다. 아래 grouped-o 규칙보다 **앞에** 두어야 한다 — 스코프 유도식은 파일 순서로 이기고,
  # V4-Flash 에서 g_o·d_g = 8·1024 = 8192 = n_h_I·c_I 로 값이 겹친다.
  # 출처: transformers deepseek_v4 DeepseekV4Indexer.__init__
  #   self.kv_proj = nn.Linear(config.hidden_size, 2 * self.head_dim)      # head_dim=index_head_dim
  #   self.q_b_proj = nn.Linear(config.q_lora_rank, self.num_heads * self.head_dim)
  - expr: "n_h_I * c_I"
    scope: "indexer|scorer"
    sym: "n_h_I*c_I"
    name: "n_h^I·c^I (Lightning Indexer 쿼리 투영 폭)"
  - expr: "2 * c_I"
    scope: "indexer|scorer"
    sym: "2*c_I"
    name: "2·c^I (Indexer kv_proj / gate_proj 폭: Ca⊕Cb 겹침 레이아웃)"
  # 바깥 CSA/HCA 압축기의 kv_proj / gate_proj / position_bias 폭. 소스 docstring 이 그대로 적어
  # 놓았다(modeling_deepseek_v4.py:587-594): "`kv_proj` / `gate_proj` / `position_bias` project to
  # `2 * head_dim`: each token contributes two independent compressed series Ca and Cb".
  # V4-Pro 는 2·512 = 1024 = o_lora_rank 라 그 폭이 `d_g`(grouped output projection 의 그룹당
  # 중간 차원)로 렌더되고 있었다 — 전혀 다른 모듈의 이름이다. C절 전수 점검 2026-08-12.
  - expr: "2 * d_head"
    scope: "compressor(?!\\.indexer)"
    sym: "2*d_head"
    name: "2·d_head (CSA/HCA 압축기 kv_proj·gate_proj 폭: Ca⊕Cb)"
  # `new_kv = chunk_kv.new_zeros((batch, n_windows, 2 * ratio, self.head_dim))`, ratio =
  # compress_rate — 창 하나가 앞 창과 겹치도록 자리를 두 배로 잡는 Ca/Cb 레이아웃이다.
  - expr: "2 * m_csa"
    scope: "indexer|scorer"
    sym: "2*m_csa"
    name: "2·m_csa (Indexer 겹침 창 슬롯 수: Ca⊕Cb)"

  # ---- DeepSeek-V4 grouped output projection ----
  # 출처: transformers deepseek_v4 DeepseekV4GroupedLinear docstring(§2.3.1 Grouped Output
  # Projection), rules/structures/attention/grouped-output-projection.md
  - expr: "g_o * d_g"
    scope: "o_a_proj|o_b_proj|self_attn"
    sym: "g_o*d_g"
    name: "g_o·d_g (grouped output projection 합친 폭 → o_b_proj 입력)"
  # unless_equals: 잔차 스트림과 값이 같으면 물러난다. V4-Flash 는 n_h·d_head/g_o = 64·512/8 =
  # 4096 = d_model 이라, `self_attn` 스코프가 덮는 indexer 안에서 잔차 폭이 이 식으로 렌더됐다
  # (`weights_proj` 가중치가 `[n_h_I, n_h*d_head/g_o]` — 소스는 `Linear(hidden_size, ...)` 다).
  - expr: "n_h * d_head // g_o"
    scope: "o_a_proj|o_b_proj|self_attn"
    unless_equals: [d_model]
    sym: "n_h*d_head/g_o"
    name: "n_h·d_head/g_o (grouped output projection 그룹당 입력 폭)"

  # ---- DeepSeek-V4 블록 압축 attention (CSA / HCA) ----
  # 압축 엔트리 수 = T/m. 코어 attention은 sliding KV 뒤에 압축 KV를 concat하고,
  # 학습형 attention sink 1열이 score 행렬에 더 붙는다(+1).
  # 출처: transformers deepseek_v4 (CSA/HCACompressor, DeepseekV4Attention.forward),
  # vLLM 블로그 2026-04-24 "c4a/c128a" 설명, rules/structures/attention/csa.md·hca.md
  - expr: "T + T // m_csa + 1"
    scope: "attn|attention"
    sym: "T+T/m_csa+1"
    name: "T + T/m_csa + 1 (CSA 레이어 score 폭: sliding KV ⊕ 압축 KV ⊕ attention sink)"
  - expr: "T + T // m_hca + 1"
    scope: "attn|attention"
    sym: "T+T/m_hca+1"
    name: "T + T/m_hca + 1 (HCA 레이어 score 폭: sliding KV ⊕ 압축 KV ⊕ attention sink)"
  - expr: "T + T // m_csa"
    scope: "attn|attention"
    sym: "T+T/m_csa"
    name: "T + T/m_csa (CSA 레이어 KV 길이: sliding ⊕ 압축 엔트리)"
  - expr: "T + T // m_hca"
    scope: "attn|attention"
    sym: "T+T/m_hca"
    name: "T + T/m_hca (HCA 레이어 KV 길이: sliding ⊕ 압축 엔트리)"
  # decode에서는 sliding 분기의 KV가 window에서 상한이 걸리므로 score 폭이 `T + …`가 아니라
  # `w_local + …`가 된다(prefill 규칙과 짝을 이룬다). 예전엔 이 값들이 휴리스틱으로 아무 이름이나
  # 받았고, 휴리스틱을 조인 뒤 정수로 드러나서 규칙으로 등재했다(자유 평가 2026-07-31).
  # V4-Pro 실측(T=2048, w_local=128, m_csa=4, m_hca=128): 640·641·144·145
  # V4-Flash 실측(T=1032): 386·387·136·137
  - expr: "w_local + T // m_csa + 1"
    scope: "attn|attention"
    sym: "w_local+T/m_csa+1"
    name: "w_local + T/m_csa + 1 (decode CSA score 폭: window KV ⊕ 압축 KV ⊕ attention sink)"
  - expr: "w_local + T // m_hca + 1"
    scope: "attn|attention"
    sym: "w_local+T/m_hca+1"
    name: "w_local + T/m_hca + 1 (decode HCA score 폭: window KV ⊕ 압축 KV ⊕ attention sink)"
  - expr: "w_local + T // m_csa"
    scope: "attn|attention"
    sym: "w_local+T/m_csa"
    name: "w_local + T/m_csa (decode CSA KV 길이)"
  - expr: "w_local + T // m_hca"
    scope: "attn|attention"
    sym: "w_local+T/m_hca"
    name: "w_local + T/m_hca (decode HCA KV 길이)"
  - expr: "T // m_hca + 1"
    scope: "attn|attention|compressor|indexer"
    sym: "T/m_hca+1"
    name: "T/m_hca + 1 (HCA block-bias 버퍼 = 압축 엔트리 수 + 무효 인덱스 슬롯 1)"
  - expr: "T // m_csa + 1"
    scope: "attn|attention|compressor|indexer"
    sym: "T/m_csa+1"
    name: "T/m_csa + 1 (CSA block-bias 버퍼 = 압축 엔트리 수 + 무효 인덱스 슬롯 1)"
  - expr: "T // m_csa - 1"
    scope: "attn|attention|compressor|indexer"
    sym: "T/m_csa-1"
    name: "T/m_csa − 1 (CSA Ca/Cb 겹침 shift: 이전 윈도우 기여분 슬라이스)"
  - expr: "T // m_csa"
    scope: "attn|attention|compressor|indexer"
    sym: "T/m_csa"
    name: "T/m_csa (CSA 압축 엔트리 수)"
  - expr: "T // m_hca"
    scope: "attn|attention|compressor|indexer"
    sym: "T/m_hca"
    name: "T/m_hca (HCA 압축 엔트리 수)"

  # `n_h*d_v` 가 **맨 앞**이다. GLM-5.2 는 d_v = 256 = d_nope+d_rope = 192+64 라 아래 MLA Q 식과
  # 값이 같고, 그때는 attention 출력 쪽이 맞다 — o_proj 의 선언 폭이 `num_heads * v_head_dim`
  # 이기 때문이다. 이 순서를 뒤집었더니 GLM-5.2 의 reshape 불일치가 0 → 156 으로 튀었다.
  - expr: "n_h * d_v"
    scope: "attn|attention"
    sym: "n_h*d_v"
    name: "n_h·d_v (attention 출력, o_proj 직전)"

  # MLA 의 Q 업투영 폭. **fused QKV 규칙보다 앞에 둔다** — 두 식이 같은 값이 되는 모델이 있고
  # (Kimi-K2.6: 64·192 = (64+2·64)·64 = 12288), 그때는 MLA 쪽이 맞다. MLA 에는 n_kv 라는 개념이
  # 없기 때문이다. d_nope/d_rope 가 없는 모델에서는 이 식이 아예 평가되지 않으므로 falcon·GPT-2·
  # Phi-4 의 진짜 fused QKV 는 영향을 받지 않는다. 출처: modeling_deepseek.py:669
  #   self.q_b_proj = nn.Linear(config.q_lora_rank, self.num_heads * self.q_head_dim)
  - expr: "n_h * (d_nope + d_rope)"
    scope: "attn|attention"
    sym: "n_h*(d_nope+d_rope)"
    name: "n_h·(d_nope+d_rope) (MLA q_b_proj 출력)"

  # ---- fused QKV 투영 (Q·K·V를 한 행렬로) ----
  # Falcon은 q,k,v를 한 번에 낸다: (n_h + 2·n_kv)·d_head.
  # 출처: transformers models/falcon/modeling_falcon.py
  #   qkv_out_dim = (num_kv_heads * 2 + num_attention_heads) * head_dim
  # 스코프에서 `attn|attention` 를 뺀다. 이 식은 **Q·K·V 를 한 행렬로 내는 모듈** 의 것이지
  # attention 안 아무 곳의 것이 아니다. 넓은 스코프 탓에 MLA 의 `q_b_proj`(= Q 저랭크 업투영,
  # `nn.Linear(q_lora_rank, num_heads * q_head_dim)`)가 이 이름을 달고 있었다 — Kimi-K2.6 은
  # (64+2·64)·64 = 64·192 = 12288 로 값이 정확히 같다. MLA 에는 n_kv 라는 개념 자체가 없다.
  # 모델 저장소 remote code 를 열게 되면서 소속 검사가 처음으로 이 모델들을 보고 잡았다
  # (2026-08-12). 진짜 fused QKV 는 `query_key_value`(falcon) / `qkv_proj`(Phi-4) /
  # `c_attn`(GPT-2) 라는 이름을 쓴다.
  - expr: "(n_h + 2 * n_kv) * d_head"
    scope: "^(?!.*q_b_proj)(?=.*(?:attn|attention|query_key_value|qkv|c_attn|Wqkv))"
    sym: "(n_h+2*n_kv)*d_head"
    name: "(n_h + 2·n_kv)·d_head (fused QKV 투영 폭 — Q·K·V 한 행렬)"
  # fused QKV를 head 축으로 펼칠 때 나오는 head 총수. Falcon-7B에서 `[B,T,73,d_head]`의
  # 73이 정수로 남아 있었다(71 Q + 1 K + 1 V) — 리뷰 패킷을 만들어 트레이스를 압축된
  # 형태로 훑다가 눈에 띄었다(2026-07-30).
  - expr: "n_h + 2 * n_kv"
    scope: "^(?!.*q_b_proj)(?=.*(?:attn|attention|query_key_value|qkv|c_attn|Wqkv))"
    sym: "n_h+2*n_kv"
    name: "n_h + 2·n_kv (fused QKV를 head 축으로 편 총 head 수: Q + K + V)"

  # ---- RoPE rotate_half 분할 축 ----
  # `rotate_half(x)`는 마지막 축을 정확히 반으로 자른다:
  #   x1 = x[..., : d//2];  x2 = x[..., d//2 :];  return cat((-x2, x1))
  # 트레이스에서 self_attn의 `slice` → `neg` → `concat` 3연속으로 나타나고, cos/sin을 만드는
  # `rotary_emb`의 inv_freq도 같은 폭이다. 즉 이 축은 **회전 차원의 절반**이지 다른 무엇도 아니다.
  # 규칙으로 등록하기 전에는 휴리스틱이 값만 맞는 이름을 붙였다 — Llama-3.1-8B `2*n_h`,
  # SmolLM3 `4*n_h`, GLM-4.5-Air·gpt-oss `4*n_kv`, DeepSeek-V3 `4*k`, tiny-deepseek-v3 `4*E`,
  # Llama-3.1-405B `n_h/2`. 전부 산술적으로는 참이라 게이트가 못 잡았고, 자유 평가에서 나왔다
  # (2026-07-31). 출처: transformers 공통 `rotate_half` / `apply_rotary_pos_emb`.
  # 부분·decoupled RoPE는 head 전체가 아니라 d_rope만 회전하므로 그쪽을 먼저 본다.
  - expr: "d_rope // 2"
    scope: "(?<!linear_)attn|attention|rotary"
    sym: "d_rope/2"
    name: "d_rope/2 (부분/decoupled RoPE의 rotate_half 분할 축)"
  - expr: "d_head // 2"
    scope: "attn|attention|rotary"
    sym: "d_head/2"
    name: "d_head/2 (RoPE rotate_half 분할 축)"

  # ---- mHC (Manifold-Constrained Hyper-Connections) ----
  # 잔차를 n_hc개 병렬 스트림으로 유지하므로 hc 모듈의 입력 폭은 n_hc·d_model이고,
  # 게이트 파라미터는 pre(n_hc) + post(n_hc) + comb(n_hc²) = (2+n_hc)·n_hc개다.
  # DeepSeek-V4-Pro 실측: `attn_hc.fn` = [24, 28672] = [(2+4)·4, 4·7168] ✓
  # 이전에는 `4*k`(24)·`4*d_model`(28672)로 찍혔다 — k는 expert top-k라 잔차 연결과 무관하다.
  # 출처: rules/structures/residual/mhc.md(파라미터 shape가 이미 이 식으로 문서화돼 있었다),
  # DeepSeek-V4 paper §2.2, Hyper-Connections (Zhu et al.).
  - expr: "n_hc * d_model"
    scope: "_hc|hyper"
    sym: "n_hc*d_model"
    name: "n_hc·d_model (mHC: n_hc개 잔차 스트림을 편 폭)"
  - expr: "(2 + n_hc) * n_hc"
    scope: "_hc|hyper"
    sym: "(2+n_hc)*n_hc"
    name: "(2+n_hc)·n_hc (mHC 게이트 파라미터 수: pre n_hc + post n_hc + comb n_hc²)"
  # sym에 `^`를 쓰면 안 된다 — 렌더된 라벨은 검증에서 그대로 eval되는데 Python `^`는 거듭제곱이
  # 아니라 XOR이다. `n_hc^2`로 뒀다가 산술 검사에서 588건이 거짓으로 잡혔다(2026-07-31).
  - expr: "n_hc * n_hc"
    scope: "_hc|hyper"
    sym: "n_hc*n_hc"
    name: "n_hc² (mHC comb 행렬 원소 수)"

  # ---- GQA repeat 계수 ----
  # repeat_kv가 KV head를 Q head 수만큼 복제할 때 나타나는 expand 축.
  # 출처: transformers 공통 `repeat_kv`, 01-main.md C7.
  - expr: "n_h // n_kv"
    scope: "attn|attention"
    sym: "n_h/n_kv"
    name: "n_h/n_kv (GQA repeat 계수 — repeat_kv의 expand 축)"

  # ---- sliding window mask ----
  - expr: "w_local - 1"
    scope: "attn|attention"
    sym: "w_local-1"
    name: "w_local − 1 (sliding window mask 밴드 폭)"

  # ---- SSM / Mamba-2 ----
  # d_inner = n_h_ssm·d_head_ssm. conv1d는 d_inner + 2·n_g·d_state를 받고,
  # in_proj는 gate+x(2·d_inner) + B,C(2·n_g·d_state) + dt(head당 1)를 한 번에 낸다.
  # 출처: transformers nemotron_h/mamba2 modeling, rules/structures/ssm/mamba.md
  # d_inner 자체. Zamba2는 이걸 `int(config.mamba_expand * hidden_size)` 로 계산하는데,
  # float 곱이라 int() 가 필요하고 그 순간 config 태그가 벗겨진다(§6.4의 원리적 손실 사례).
  # 검증된 식으로 되찾는다: d_inner = n_h_ssm · d_head_ssm.
  # 출처: transformers/models/zamba2/modeling_zamba2.py:420,436 (self.intermediate_size 가
  #       conv_dim = intermediate_size + 2·n_groups·ssm_state_size 의 첫 항).
  - expr: "n_h_ssm * d_head_ssm"
    scope: "mamba|mixer|ssm|conv1d"
    sym: "d_inner"
    name: "d_inner (Mamba 내부 폭 = n_h_ssm · d_head_ssm)"

  # conv1d 입력 폭. 위 주석대로 conv1d 는 d_inner + 2·n_g·d_state 를 받는다. 규칙이 없으면
  # d_inner 쪽 태그가 int() 로 죽은 모델에서 `(4096+2*d_state)` 처럼 반쪽짜리 식이 남는다.
  - expr: "n_h_ssm * d_head_ssm + 2 * n_g_ssm * d_state"
    scope: "mamba|mixer|ssm|conv1d"
    sym: "d_inner+2*n_g*d_state"
    name: "d_inner + 2·n_g·d_state (conv1d 입력 폭)"

  - expr: "2 * n_h_ssm * d_head_ssm + 2 * n_g_ssm * d_state + n_h_ssm"
    scope: "mamba|mixer|ssm|in_proj"
    sym: "2*d_inner+2*n_g*d_state+n_h_ssm"
    name: "2·d_inner + 2·n_g·d_state + n_h_ssm (Mamba in_proj 출력: gate+x, B+C, dt)"
  - expr: "n_h_ssm * d_head_ssm + 2 * n_g_ssm * d_state"
    scope: "mamba|mixer|ssm|conv1d|act"
    sym: "d_inner+2*n_g*d_state"
    name: "d_inner + 2·n_g·d_state (Mamba causal conv1d 폭: x, B, C)"
  - expr: "n_h_ssm * d_head_ssm"
    scope: "mamba|mixer|ssm"
    sym: "d_inner"
    name: "n_h_ssm·d_head_ssm (Mamba d_inner)"
  # Mamba2 in_proj 는 x, conv(B·C 포함), dt 를 한 번에 낸다.
  # 출처: modeling_granitemoehybrid.py:534
  #   projection_size = self.intermediate_size + self.conv_dim + self.num_heads
  - expr: "n_h_ssm * d_head_ssm + (n_h_ssm * d_head_ssm + 2 * n_g_ssm * d_state) + n_h_ssm"
    scope: "mamba|mixer|ssm|in_proj"
    sym: "d_inner+conv_dim+n_h_ssm"
    name: "d_inner + conv_dim + n_h_ssm (Mamba2 in_proj 출력: x ⊕ conv ⊕ dt)"
  # 공유 MLP 의 gate+up 융합 폭.
  # 출처: modeling_granitemoehybrid.py:744 `nn.Linear(self.input_size, self.hidden_size * 2)`
  - expr: "2 * d_shared"
    scope: "shared"
    sym: "2*d_shared"
    name: "2·d_shared (공유 MLP gate+up 융합 투영 폭)"

  # ---- 선형 attention (Gated DeltaNet) ----
  # in_proj_qkvz는 q,k(각 n_k·d_k)와 v,z(각 n_v·d_v)를 한 번에 낸다.
  # 출처: transformers qwen3_next modeling, rules/structures/attention/gated-deltanet.md
  - expr: "2 * n_h_lin_k * d_head_lin_k + 2 * n_h_lin_v * d_head_lin_v"
    scope: "linear_attn|in_proj"
    sym: "2*n_k*d_k+2*n_v*d_v"
    name: "2·(n_k·d_k) + 2·(n_v·d_v) (DeltaNet in_proj_qkvz 출력: q,k,v,z)"
  - expr: "n_h_lin_k * d_head_lin_k + 2 * n_h_lin_v * d_head_lin_v"
    scope: "linear_attn|in_proj"
    sym: "n_k*d_k+2*n_v*d_v"
    name: "n_k·d_k + 2·(n_v·d_v)"
  # 그 융합 투영이 갈라지는 조각들. 소스가 이름을 갖고 있다(modeling_qwen3_next.py:520-521,
  # 638-644): `key_dim = head_k_dim * num_k_heads`, `value_dim = head_v_dim * num_v_heads`,
  # `split(mixed_qkv, [key_dim, key_dim, value_dim], dim=-1)`.
  # Qwen3-Next 는 key_dim = 16·128 = 2048 = d_model 이고 value_dim = 32·128 = 4096 = n_h·d_head 라
  # 두 조각이 전부 softmax-attention 이름으로 렌더됐다 — 값으로는 구별할 수 없다.
  # unless_equals: Qwen3-Next 는 key_dim = 16·128 = 2048 = d_model 이다. 이 식을 무조건 쓰게 하면
  # `linear_attn` 으로 **들어오는 잔차 스트림**까지 key_dim 으로 렌더되어, 레이어 루트가 d_model
  # 이라고 부르는 바로 그 텐서를 한 칸 안에서 다른 이름으로 부른다(flow_ambig 0→72). 잔차 폭이
  # 우선이다 — 조각 이름 하나를 잃는 것보다 모델에서 가장 근본적인 축을 지키는 쪽이 낫다.
  - expr: "n_h_lin_k * d_head_lin_k"
    scope: "linear_attn"
    unless_equals: [d_model]
    sym: "n_k*d_k"
    name: "n_k·d_k (DeltaNet key_dim — q/k 조각 폭)"
  - expr: "n_h_lin_v * d_head_lin_v"
    scope: "linear_attn"
    sym: "n_v*d_v"
    name: "n_v·d_v (DeltaNet value_dim — v/z 조각 폭)"
  # `query.repeat_interleave(num_v_heads // num_k_heads, dim=2)` (:653-655) — GQA 의 n_h/n_kv 와
  # 같은 역할이고, 값(2)이 n_kv 와 겹쳐 그 이름으로 렌더되고 있었다.
  - expr: "n_h_lin_v // n_h_lin_k"
    scope: "linear_attn"
    sym: "n_v/n_k"
    name: "n_v/n_k (DeltaNet key head 하나가 담당하는 value head 수)"
  # 융합 투영을 key head 별로 접을 때의 head 당 폭. 소스가 식 그대로 적어 놓았다
  # (`fix_query_key_value_ordering`, modeling_qwen3_next.py:562-565):
  #   new_tensor_shape_qkvz = (..., num_k_heads,
  #                            2*head_k_dim + 2*head_v_dim*num_v_heads//num_k_heads)
  # Qwen3-Next 는 2·128 + 2·128·2 = 768 이고, 어떤 심볼과도 겹치지 않아 정수로 남아 있었다.
  - expr: "2 * d_head_lin_k + 2 * d_head_lin_v * n_h_lin_v // n_h_lin_k"
    scope: "linear_attn"
    sym: "2*d_k+2*(n_v/n_k)*d_v"
    name: "2·d_k + 2·(n_v/n_k)·d_v (DeltaNet qkvz 를 key head 별로 접은 폭)"
  # 그 폭이 갈라지는 v/z 조각: `num_v_heads // num_k_heads * head_v_dim` (:573-574).
  # 값(256)이 softmax attention 의 d_head 와 겹쳐 그 이름으로 렌더됐다.
  - expr: "d_head_lin_v * n_h_lin_v // n_h_lin_k"
    scope: "linear_attn"
    sym: "(n_v/n_k)*d_v"
    name: "(n_v/n_k)·d_v (DeltaNet v/z 조각 — key head 하나가 들고 있는 value 폭)"
  # `projection_size_ba = self.num_v_heads * 2` (:540) — b 와 a 두 게이트를 head 마다 하나씩.
  - expr: "2 * n_h_lin_v"
    scope: "linear_attn"
    sym: "2*n_v"
    name: "2·n_v (DeltaNet in_proj_ba 출력 폭: beta 와 alpha 게이트)"
  # ---- SSM 의 그룹 확장 배수 ----
  # Mamba2 계열은 state 를 n_g_ssm 그룹만 두고 head 전체가 공유한다 — attention 의 GQA 와 같은
  # 구조이고, 확장 배수는 n_h/n_g_ssm 이다. 트레이스가 그대로 보여준다(Nemotron-3-Nano,
  # n_h=40, n_g_ssm=8):
  #   expand [B,n_g_ssm,1,T,d_state] -> [B,n_g_ssm,5,T,d_state] -> view -> [B,n_h,T,d_state]
  # 즉 n_g_ssm x 5 = n_h. 이름이 없어 정수 5 로 남아 있었다.
  - expr: "n_h // n_g_ssm"
    scope: "mixer|mamba|ssm"
    sym: "n_h/n_g_ssm"
    name: "n_h/n_g_ssm (SSM state 그룹 하나가 담당하는 head 수 — GQA 의 n_h/n_kv 와 같은 역할)"

  # ---- decode 의 causal conv 캐시 길이 ----
  # Mamba 의 causal conv1d 는 폭 d_conv 를 보므로, decode 에서 캐시된 d_conv 개에 새 토큰 1개를
  # 붙였다가 다시 마지막 d_conv 개로 자른다. 트레이스가 그대로 보여준다(Zamba2, d_conv=4):
  #   concat [...] -> [B, d_inner+2*n_g*d_state, 5] -> slice -> [B, ..., d_conv]
  # KV 캐시의 T+1 과 정확히 같은 구조다.
  - expr: "d_conv + 1"
    scope: "mixer|mamba|conv"
    sym: "d_conv+1"
    name: "d_conv+1 (decode 의 conv 캐시 — 캐시 d_conv 개 + 새 토큰 1개)"

  # ---- decode 의 KV 캐시 길이 ----
  # decode 단계에서 attention 이 보는 컨텍스트 길이는 캐시에 든 T개 + 새 토큰 1개다. 트레이스가
  # 그대로 보여준다(Llama-3.1-8B, T=16):
  #   concat [B,n_kv,T,d_head] + [B,n_kv,1,d_head] -> [B,n_kv,T+1,d_head]   구체 [1,8,17,128]
  # prefill 에는 한 건도 없고 decode 에만 800건 나온다.
  # 등록하는 이유: 휴리스틱 `심볼+1` 가지가 이 이름을 만들어내고 있었다. 값은 맞지만 근거가
  # 규칙이 아니라 산술이라 `heur_plus1` 로 집계됐다(함대 36,813축, 전체의 0.69%).
  # 스코프에 루트 모듈을 넣은 이유: decode 마스크는 레이어가 아니라 모델 루트(`model` /
  # gpt2 계열은 `transformer`)에서 한 번 만들어진다. 거기서는 attn 스코프에 안 걸려 같은 값이
  # 다시 산술 휴리스틱(heur_plus1)으로 지어지고 있었다 — gpt-oss 20b/120b, falcon 각 15축.
  # `mixer` 도 넣는 이유: nemotron_h 는 어텐션 층과 Mamba 층을 모두 `mixer` 라고 부른다.
  # 그래서 어텐션 층의 KV concat(`[B, n_kv, T+1, d_state]`)이 attn 스코프에 안 걸려 매번
  # 산술로 다시 지어지고 있었다(Nemotron-3 Super/Ultra, 2026-08-10).
  - expr: "T + 1"
    scope: "attn|attention|mixer|^model$|^transformer$"
    sym: "T+1"
    name: "T+1 (decode 의 KV 캐시 길이 — 캐시 T개 + 새 토큰 1개)"

  # ---- MoE 라우팅 슬롯 수 ----
  # 라우터는 토큰마다 expert를 k개 고르므로 라우팅된 (토큰, 슬롯) 쌍은 T·k개다. 모델 코드가
  # `topk_idx.view(-1)` 로 평탄화하는 바로 그 축이고, 이후 sort/index/gather가 전부 이 길이를
  # 쓴다. 출처: transformers 의 각 MoE forward (deepseek_v3 / glm4_moe / gpt_oss).
  # 등록하는 이유: 이 값이 E(=128, GLM-4.5-Air)나 4*d_moe(=12288, DeepSeek-V4-Pro)와 우연히
  # 같아지는 모델이 있어 값만으로는 못 가린다. prefill/decode 비교(src/tdep.py)가 이 축이
  # 시퀀스 길이에 따라 변한다는 것을 보여주면 고정 심볼은 후보에서 빠지는데, 그때 이름을 줄
  # 등록 규칙이 없으면 휴리스틱 곱으로 떨어진다(GLM heur 3,155 -> 5,630).
  - expr: "k * T"
    scope: "expert|moe|router|gate(?!_proj)|feed_forward"
    sym: "k*T"
    name: "k·T (라우팅된 (토큰, 슬롯) 쌍 수 — 토큰마다 expert k개)"

  # sym spells T LAST, matching symbolic_shape.dim()'s canonical product order -- otherwise the
  # same tensor reads `T*n_h_lin_v` from this rule and `n_h_lin_v*T` everywhere else, which the
  # reshape cross-check reported 72 times on Qwen3-Next.
  - expr: "T * n_h_lin_v"
    scope: "linear_attn|norm"
    sym: "n_h_lin_v*T"
    name: "T·n_h_lin_v (value head 축까지 flatten — gated norm 입력)"

  # ---- 부분 RoPE의 rotate_half (d_rope 필드가 없고 계수만 있는 모델) ----
  # GLM-4.5-Air는 `qk_rope_head_dim` 같은 필드 없이 `partial_rotary_factor=0.5`만 준다.
  # 회전 차원 = round(d_head·pr) = 64이고 rotate_half는 그 절반인 32를 자른다. 규칙이 없어서
  # 32가 `4*n_kv`(=4·8)로 찍혀 있었다 — expert도 KV head도 아닌 축이다(자유 평가 2026-07-31).
  - expr: "round(d_head * pr) // 2"
    scope: "attn|attention|rotary"
    sym: "d_rope/2"
    name: "d_rope/2 (partial_rotary_factor 기준 rotate_half 분할 축)"
  # 회전 차원 자체. `qk_rope_head_dim` 필드가 있는 모델은 d_rope 심볼로 바로 잡히지만,
  # 계수만 주는 모델(Qwen3-Next pr=0.25·d_head=256 → 64, GLM pr=0.5·128 → 64)은 규칙이 필요하다.
  # 스코프에서 `linear_attn` 을 뺀다: 선형 attention 에는 RoPE 가 없는데 `attn` 이 그 이름 안에서
  # 매치했고, Qwen3-Next 는 round(256·0.25) = 64 = 2·num_v_heads 라 `in_proj_ba` 의 출력 폭을
  # 회전 차원으로 렌더했다(소스는 `projection_size_ba = self.num_v_heads * 2`, :540).
  # 주석이 "스코프에서 linear_attn 을 뺀다"고 적어 두었는데 **정규식은 바뀐 적이 없었다**
  # (`attn` 은 `linear_attn` 안에서도 매치한다). Qwen3-Next 계열의 gated delta rule 블록에는
  # RoPE 가 아예 없고 -- RoPE 는 그 스택의 `self_attn` 레이어에만 있다 -- 그런데
  # partial_rotary_factor(0.25) x head_dim(256) = 64 가 그 블록의 chunk_size 와 같아서 청크
  # 스캔이 통째로 회전 차원으로 렌더되고 있었다(Qwen3.6-27B 31,440축). 2026-08-13 에 한 번
  # 고쳤다가 되돌렸는데(그때는 이름이 칸에 붙어 있어 이웃이 따라오지 못했다), 축 등가류 통일
  # 이후에는 깨끗하게 들어간다. 2026-08-14.
  - expr: "round(d_head * pr)"
    scope: "(?<!linear_)attn|attention|rotary"
    sym: "d_rope"
    name: "d_rope (partial_rotary_factor 기준 회전 차원)"

  # ---- Mamba-2 게이트 정규화 / conv 패딩 (Nemotron) ----
  # 출처: transformers nemotron_h/mamba2 modeling, rules/structures/ssm/mamba.md
  # gated RMSNorm은 그룹 단위로 걸린다 → 마지막 축이 d_inner/n_g.
  # causal conv1d는 왼쪽으로 (d_conv-1)만큼 패딩하므로 시퀀스 축이 T + d_conv - 1이 된다.
  - expr: "n_h_ssm * d_head_ssm // n_g_ssm"
    scope: "mamba|mixer|ssm|norm"
    sym: "d_inner/n_g"
    name: "d_inner/n_g_ssm (Mamba gated RMSNorm의 그룹당 폭)"
  - expr: "n_g_ssm * d_state"
    scope: "mamba|mixer|ssm|conv1d"
    sym: "n_g*d_state"
    name: "n_g_ssm·d_state (B/C 하나의 폭)"
  # 스코프에 `conv` (conv1d 뿐 아니라 short-conv 블록 전체). LFM2 의 `Lfm2MoeShortConv` 는
  # `nn.Conv1d(..., padding=self.conv_kernel_size - 1)`(modeling_lfm2_moe.py:420-427) 이라
  # 정확히 같은 식인데 모듈 이름이 `conv.conv` 라 `conv1d` 정규식에 안 걸렸다. T=16, d_conv=3 →
  # 18 이 이름 없는 정수로 남아 있었고 판정에는 "소스에서 근거를 못 찾았다"고 적혀 있었다 —
  # 근거는 소스에 있었고 규칙도 이미 있었다(2026-08-13).
  - expr: "T + d_conv - 1"
    scope: "mamba|mixer|ssm|conv"
    sym: "T+d_conv-1"
    name: "T + d_conv − 1 (causal conv1d 좌측 패딩 포함 길이)"

  # ---- xLSTM q/k/v 투영 폭 ----
  # 출처: NX-AI xlstm 공식 구현 — q/k는 d_model·qk_dim_factor, v는 d_model·v_dim_factor.
  # xLSTM-7b는 qk_dim_factor=0.5라 q/k 폭이 2048이고, 규칙이 없을 때는 `4*d_head`로 찍혔다.
  - expr: "d_model * qk_f"
    scope: "mlstm|xlstm|\\.q$|\\.k$|q_|k_"
    sym: "d_model*qk_f"
    name: "d_model·qk_dim_factor (xLSTM q/k 투영 폭)"
  - expr: "d_model * v_f"
    scope: "mlstm|xlstm|\\.v$|v_"
    sym: "d_model*v_f"
    name: "d_model·v_dim_factor (xLSTM v 투영 폭)"
  # head당 폭. xLSTM-7b는 4096·0.5/8 = 256이고, 규칙이 없을 때는 `T*T`로 찍혔다(T=16이라 우연히
  # 256). 시퀀스 길이가 바뀌면 즉시 거짓이 되는 라벨이었다.
  - expr: "d_model * qk_f // n_h"
    scope: "mlstm|xlstm|mlstm_backend"
    sym: "d_model*qk_f/n_h"
    name: "d_model·qk_dim_factor/n_h (xLSTM head당 q/k 차원)"

  # ---- xLSTM FFN ----
  # 출처: NX-AI xlstm 공식 구현 — up-projection 폭 = round_up(d_model·ffn_proj_factor, 단위)
  - expr: "ceil(d_model * ffn_f / ffn_r) * ffn_r"
    scope: "ffn|mlp|proj_up|proj_down|act"
    sym: "roundup(d_model*ffn_f,ffn_r)"
    name: "round_up(d_model·ffn_proj_factor, ffn_round_up_to_multiple_of) (xLSTM FFN 폭)"

  # ---- MLA 합성 차원 (Phase 6에서 확인, 기존 _known_composites에서 이관) ----
  - expr: "n_h * (d_nope + d_v)"
    scope: "attn|attention"
    sym: "n_h*(d_nope+d_v)"
    name: "n_h·(d_nope+d_v) (MLA kv_b_proj 출력)"
  # `n_h*d_v` 를 `n_h*(d_nope+d_rope)` 보다 **앞에** 둔다. 두 식이 같은 값이 되는 모델이 있고
  # (GLM-5.2: d_v = 256 = d_nope+d_rope = 192+64), 그때는 attention 출력 쪽이 맞다 — o_proj 의
  # 선언 폭이 `num_heads * v_head_dim` 이기 때문이다. 순서가 반대라 GLM-5.2 의 o_proj 직전 축
  # 78행이 q_b_proj 출력 폭의 이름을 달고 있었다(C절 전수 점검 2026-08-12).
  # DeepSeek-V2/V3 계열은 d_v(128) != d_nope+d_rope(192) 라 이 순서 변경의 영향을 받지 않는다.
  - expr: "c_kv + d_rope"
    scope: "attn|attention"
    sym: "c_kv+d_rope"
    name: "c_kv+d_rope (MLA kv_a_proj_with_mqa 출력)"
  - expr: "n_h * d_nope"
    scope: "attn|attention"
    sym: "n_h*d_nope"
    name: "n_h·d_nope"
  # unless_equals: DeepSeek-V4-Flash 는 n_h·d_rope = 64·64 = 4096 = d_model 이라, 이 식이 indexer
  # 안에서 잔차 스트림을 가져갔다 — 소스는 `nn.Linear(config.hidden_size, ...)` 이므로 그 축의
  # 이름은 d_model 이다. RoPE 폭의 곱이 잔차 폭과 같아지는 것은 우연이다.
  - expr: "n_h * d_rope"
    scope: "attn|attention|indexer"
    unless_equals: [d_model]
    sym: "n_h*d_rope"
    name: "n_h·d_rope"
  - expr: "d_nope + d_rope"
    scope: "attn|attention"
    sym: "d_nope+d_rope"
    name: "d_nope+d_rope (q/k head 전체 차원)"
  - expr: "d_nope + d_v"
    scope: "attn|attention"
    sym: "d_nope+d_v"
    name: "d_nope+d_v"

  # ---- 공통 투영 폭 ----
  - expr: "n_h * d_head"
    scope: "attn|attention|o_proj|q_proj|k_proj|v_proj"
    sym: "n_h*d_head"
    name: "n_h·d_head (Q 투영 폭 / attention 출력 폭)"
  - expr: "n_kv * d_head"
    scope: "attn|attention|k_proj|v_proj"
    sym: "n_kv*d_head"
    name: "n_kv·d_head (KV 투영 폭)"
  - expr: "2 * n_kv * d_head"
    scope: "attn|attention|k_proj|v_proj"
    sym: "2*n_kv*d_head"
    name: "2·n_kv·d_head (K와 V 합친 투영 폭)"
  # E·k(토큰당 라우팅되는 (expert,slot) 쌍 총수)는 MoE 경로에서만 의미가 있다. 스코프가 없어서
  # **전역 파생 규칙**으로 동작했고, self_attn 안에서 값이 우연히 같은 축을 가로챘다 —
  # gpt-oss-20b는 E·k = 32·4 = 128 == sliding_window라 decode sliding 레이어의 attention
  # 컨텍스트 길이가 `E*k`로 찍혔다(외부 검토 2026-07-30). E와 k 자신의 스코프와 같은 값으로 맞춘다.
  # 스코프가 있어도 부족했다: MoE 블록 자체가 그 스코프 안이라, 블록을 그냥 통과하는 잔차
  # 스트림이 E·k 와 값이 같으면 그쪽으로 붙고 뒤따르는 norm 까지 전파된다. Qwen3.6-35B-A3B 는
  # E·k = 256·8 = 2048 = d_model 이라 레이어 직속 LayerNorm 의 폭이 `E*k` 로 찍혔다(게이트의
  # resid_norm 불변식이 잡았다, 2026-08-09). 값으로 못 가리면 유도식은 물러난다.
  - expr: "E * k"
    scope: "expert|moe|router|gate(?!_proj)"
    unless_equals: [d_model]
    sym: "E*k"
    name: "E·k (토큰당 라우팅 (expert,slot) 쌍 총수)"

  # ---- attention sink (gpt-oss) ----
  # sink는 K/V를 늘리지 않고 **score 행렬에만** 학습형 로짓 1열을 더한다. 그래서 같은 op 쌍에서
  # bmm의 K축(= 실제 KV 길이)과 softmax 폭이 정확히 n_sink만큼 어긋난다:
  #   decode full 레이어    bmm K축 = T+1 (캐시 T + 새 토큰 1)  →  softmax = T+1+n_sink
  #   decode sliding 레이어 bmm K축 = w_local (캐시가 window 상한) →  softmax = w_local+n_sink
  # prefill에는 규칙을 두지 않는다 — 거기서 T+1은 sink지만 decode에서 T+1은 KV 길이라서, 단계
  # 구분이 없는 derived_dims에 넣으면 decode의 KV 길이를 sink로 오라벨한다.
  # 출처: transformers models/gpt_oss/modeling_gpt_oss.py (GptOssAttention.sinks를 score에 concat),
  # rules/structures/attention/attention-sink.md
  - expr: "T + 1 + n_sink"
    scope: "attn|attention"
    sym: "(T+1)+n_sink"
    name: "(T+1) + n_sink (decode full 레이어 score 폭: KV 길이 ⊕ attention sink)"
  - expr: "w_local + n_sink"
    scope: "attn|attention"
    sym: "w_local+n_sink"
    name: "w_local + n_sink (decode sliding 레이어 score 폭: window KV ⊕ attention sink)"

  # ---- MoE 전문가 FFN 폭 (③ 라벨 검토 2026-08-09) ----
  # 공유 전문가가 먼저다. 스코프가 겹치면 목록 순서가 이기므로, 더 좁은 shared_expert 를
  # 위에 둔다 -- DeepSeek-V2-Lite 는 n_shared_experts=2 라서 두 식의 값이 2816 으로 똑같고,
  # 순서가 뒤바뀌면 공유 전문가 폭이 "gate+up 융합"으로 잘못 설명된다.
  # 출처: transformers modeling_deepseek_v2.py:127 / deepseek_v3.py:223 / glm4_moe.py:392
  #   DeepseekV2MLP(config=config, intermediate_size=config.moe_intermediate_size * config.n_shared_experts)
  - expr: "E_shared * d_moe"
    scope: "shared_expert"
    sym: "E_shared*d_moe"
    name: "E_shared·d_moe (공유 전문가 FFN 폭 — 공유 전문가 수만큼 넓힌 하나의 MLP)"

  # 라우팅 전문가는 gate 와 up 을 파라미터 하나에 이어 붙여 들고 있다. 그래서 폭이 2·d_moe 다.
  # OLMoE(2·1024=2048=d_model)와 DeepSeek-V4-Flash(2·2048=4096=d_model)는 이 폭이 hidden size
  # 와 우연히 같다. 거기서는 `unless_equals: [d_model]` 로 이 규칙을 아예 재우고 기존 라벨을
  # 그대로 둔다 -- 같은 모듈 안에 잔차 스트림([T, d_model])과 gate_up 출력([k*T, 2*d_moe])이
  # 같이 있어서 값만으로는 가릴 수 없고, 스코프로 이기면 잔차 스트림까지 2*d_moe 로 바뀌기
  # 때문이다(실측: flow_ambig 32->64, 258->430). 그 두 모델의 전문가 가중치가 여전히
  # [E, d_model, d_model] 로 렌더되는 것은 확인된 오라벨이며, 올바른 해법은 nn.Parameter 의
  # 선언 폭을 읽는 앵커다(review_findings.md 참조). 값으로 우기지 않는다.
  # 출처: transformers modeling_olmoe.py:297, deepseek_v3.py:181, deepseek_v4.py:992,
  #   qwen3_moe.py:218, glm4_moe.py:350 -- 모두
  #     gate_up_proj = nn.Parameter(torch.empty(num_experts, 2 * intermediate_dim, hidden_dim))
  #   gpt_oss.py:75 는 축 순서만 다르다 (num_experts, hidden_size, 2 * intermediate_size).
  - expr: "2 * d_moe"
    scope: "expert|moe"
    unless_equals: [d_model]
    sym: "2*d_moe"
    name: "2·d_moe (라우팅 전문가 gate+up 융합 투영 폭)"

  # dense FFN 쪽의 같은 융합. Zamba2 는 MoE 가 아니라 평범한 FFN 인데 gate 와 up 을 파라미터
  # 하나로 합쳐 둔다: modeling_zamba2.py:876 `self.gate_up_proj = nn.Linear(self.hidden_size,
  # 2 * self.intermediate_size, ...)`. 위 규칙은 scope 가 `expert|moe` 라 여기 안 걸려서
  # 16384 가 이름 없는 정수로 남아 있었고, 판정에는 "일반화하려면 다른 모델 사례가 더 필요하다"고
  # 적혀 있었다 — 사례가 아니라 **소스 한 줄**이면 되는 자리였다(2026-08-13).
  # d_ff 와 같은 음의 전방탐색으로 expert/router 는 계속 배제한다.
  - expr: "2 * d_ff"
    scope: "^(?=.*(?:mlp|feed_forward|ffn|gate_up_proj))(?!.*(?:expert|router))"
    unless_equals: [d_model]
    sym: "2*d_ff"
    name: "2·d_ff (dense FFN gate+up 융합 투영 폭)"

  # ---- MoE 게이트의 그룹 분할 (③ 라벨 검토 2026-08-09) ----
  # DeepSeek 계열 게이트는 전문가 점수를 그룹으로 접어 그룹 단위 top-k 를 먼저 한다:
  #   [T, E] -> [T, n_grp, E/n_grp] -> topk(topk_group)
  # tiny-deepseek-v3 는 E=8, n_grp=2 라서 E/n_grp=4 가 2·n_grp 와 값이 같아 후자로 지어졌다.
  # 출처: transformers modeling_deepseek_v3.py:149-155 (group_scores / topk_group)
  - expr: "E // n_grp"
    scope: "gate|router"
    sym: "E/n_grp"
    name: "E/n_grp (게이트 그룹 하나가 담는 전문가 수)"

  # ---- Mamba2 SSM 그룹 확장 / 청크 상태 수 (③ 라벨 검토 2026-08-09) ----
  # 위쪽 n_h/n_g_ssm 규칙과 같은 구조지만 head 축이 attention 이 아니라 SSM 이다. 하이브리드
  # 모델은 두 head 수가 따로 있어(Nemotron-3-Nano: n_h=40, n_h_ssm=96) 식이 갈린다. 실제 축은
  #   expand [B,T,n_g_ssm,1,d_state] -> [B,T,n_g_ssm,12,d_state] -> view -> [B,T,n_h_ssm,d_state]
  # 이고 96/8=12 다. 12 는 3·d_conv(=3·4) 와도 같아 그쪽으로 지어져 있었다.
  - expr: "n_h_ssm // n_g_ssm"
    scope: "mixer|mamba|ssm"
    sym: "n_h_ssm/n_g_ssm"
    name: "n_h_ssm/n_g_ssm (SSM state 그룹 하나가 담당하는 SSM head 수)"

  # 청크 간 재귀는 청크 경계 상태를 하나 더 들고 시작하므로 축이 n_chunks+1 이다.
  # 출처: transformers modeling_nemotron_h.py:320
  #   decay_chunk = torch.exp(segment_sum(F.pad(A_cumsum[:, :, :, -1], (1, 0)))).transpose(1, 3)
  #   -> [B, n_h_ssm, n_chunks+1, n_chunks+1]
  # 규칙으로 등록하지 않는다. 우리가 트레이스하는 seq_len 은 항상 d_chunk 보다 작아 n_chunks 가
  # 늘 1 이고, 축은 말 그대로 정수 2 다 -- 관측한 적 없는 일반형(ceil(T/d_chunk)+1)을 이름으로
  # 박으면 확인하지 않은 것을 주장하게 된다. 게다가 verify_all._label_checks 의 ev() 는 `/` 를
  # floor division 으로 바꾸므로 ceil(T/d_chunk) 는 그 검사기에서 애초에 성립하지 않는다.
  # 잘못된 이름(`k`)이 붙던 원인은 따로 고쳤다: symbolic_shape.dim 의 out-of-scope 폴백이
  # group 태그 심볼을 더는 쓰지 않는다.

  # ---- Qwen3-Next linear attention (gated delta net) 의 묶음 폭 (③ 라벨 검토 2026-08-09) ----
  # 이 세 폭은 config 필드 하나로 안 떨어지고 head 수 x head 폭의 합으로만 나온다. 등록 전에는
  # d_model 의 배수로 지어지고 있었다 — conv_dim=8192 가 4·d_model(=4·2048)과 값이 같았다.
  # 출처: transformers models/qwen3_next/modeling_qwen3_next.py:520-544
  #   key_dim   = head_k_dim * num_k_heads
  #   value_dim = head_v_dim * num_v_heads
  #   conv_dim  = key_dim * 2 + value_dim                 (conv1d in/out 채널)
  #   projection_size_qkvz = key_dim * 2 + value_dim * 2  (in_proj_qkvz 출력)
  #   projection_size_ba   = num_v_heads * 2              (in_proj_ba 출력)
  - expr: "2 * n_h_lin_k * d_head_lin_k + n_h_lin_v * d_head_lin_v"
    scope: "linear_attn|conv"
    sym: "2*d_k_lin+d_v_lin"
    name: "2·key_dim + value_dim (gated delta net conv1d 채널 폭)"
  - expr: "2 * n_h_lin_k * d_head_lin_k + 2 * n_h_lin_v * d_head_lin_v"
    scope: "linear_attn"
    sym: "2*d_k_lin+2*d_v_lin"
    name: "2·key_dim + 2·value_dim (in_proj_qkvz 출력 폭: q,k,v,z 묶음)"
  - expr: "2 * n_h_lin_v"
    scope: "linear_attn"
    sym: "2*n_h_lin_v"
    name: "2·n_h_lin_v (in_proj_ba 출력 폭: beta 와 alpha 각 head 하나씩)"

  # ---- 게이트 어텐션의 Q 투영 폭 (Qwen3.5/3.6, ③ 라벨 검토 2026-08-09) ----
  # Q 투영이 query 와 gate 를 한 번에 내서 폭이 두 배다. 뒤따르는 view 는 이미
  # `[B, T, n_h, 2*d_head]` 로 맞게 렌더되고 있었고, 묶인 폭 자체만 이름이 없었다.
  # 출처: transformers models/qwen3_5/modeling_qwen3_5.py:641-643
  #   self.q_proj = nn.Linear(config.hidden_size,
  #                           config.num_attention_heads * self.head_dim * 2, bias=...)
  - expr: "2 * n_h * d_head"
    scope: "attn|attention"
    sym: "2*n_h*d_head"
    name: "2·n_h·d_head (게이트 어텐션 Q 투영: query ⊕ gate 묶음 폭)"

  # ---- Qwen3.5/3.6 linear attention 의 key_dim / value_dim — 등록 보류 ----
  # `[B, T, 2*n_kv*d_head]` 로 렌더되는 in_proj_qkvz 조각(Qwen3.6-27B 에서 2048)은 실제로는
  # key_dim = head_k_dim · num_k_heads = 16·128 이다(modeling_qwen3_5.py:520-521). 어텐션 head
  # 수와 무관한 축인데 2·4·256 과 값이 같아 그쪽으로 붙었다 — 확인된 오라벨이다.
  # 그런데 `n_h_lin_k * d_head_lin_k` / `n_h_lin_v * d_head_lin_v` 로 등록하자 Qwen3-Next 의
  # flow_ambig 가 0 -> 72 로 퇴행했다(2026-08-10). 새 이름이 붙은 축의 하류 소비자들이 옛 이름을
  # 그대로 들고 있어 한 텐서가 두 이름을 갖게 된다 — 라벨 자체가 아니라 전파가 모자란 것이다.
  # 값으로 우기지 않고 보류한다. review_findings.json 에 status:"open" 으로 실려 있다.

  # ---- mHC 잔차 스택 폭 (③ 라벨 검토 2026-08-10) ----
  # hyper-connection 은 잔차를 n_hc 개 병렬 스트림으로 들고 다닌다. 그 스택을 한 줄로 펴면
  # 폭이 n_hc·d_model 이고, hc_head 가 거기서 스트림별 혼합 가중치 n_hc 개를 뽑는다.
  # 실측(DeepSeek-V4-Flash-0731): matmul [T, 16384] @ [16384, 4] — 16384 = 4·4096.
  # 등록 전에는 n_hc 가 4 라서 4*d_model 로 지어졌다(어텐션 head 와 무관한 축이다).
  - expr: "n_hc * d_model"
    scope: "hc|hyper"
    sym: "n_hc*d_model"
    name: "n_hc·d_model (mHC 병렬 잔차 스트림을 한 줄로 편 폭)"

  # ---- sliding window decode score 폭 (③ 라벨 검토 2026-08-10) ----
  # decode 에서 sliding 레이어의 score 는 캐시(window 상한) + 새 토큰 1 이다. gpt-oss 는 여기에
  # attention sink 가 한 열 더 붙어 `w_local + n_sink` 규칙이 이미 있고, sink 가 없는 모델은
  # 그냥 +1 이다. 실측(DeepSeek-V4-Flash-0731 decode): softmax [B, n_h, 1, 129], w_local=128.
  - expr: "w_local + 1"
    scope: "attn|attention"
    sym: "w_local+1"
    name: "w_local + 1 (decode sliding 레이어 score 폭: window 캐시 ⊕ 새 토큰)"

  # ---- dense FFN 의 gate+up 융합 폭 (③ 전수 점검 2026-08-10) ----
  # MoE 전문가의 `2*d_moe` 와 같은 구조인데 dense FFN 쪽이다. MoE 스코프(expert|moe)에 안 걸려서
  # Zamba2 의 이 폭이 산술 휴리스틱으로만 이름을 받고 있었다.
  # 출처: transformers models/zamba2/modeling_zamba2.py:876
  #   self.gate_up_proj = nn.Linear(self.hidden_size, 2 * self.intermediate_size, bias=...)
  # unless_equals: d_model 과 값이 같은 모델에서는 잔차 스트림과 구별할 수 없으므로 물러난다
  # (OLMoE 에서 2*d_moe 가 잔차를 가져갔던 것과 같은 사고를 막는다).
  - expr: "2 * d_ff"
    scope: "mlp|feed_forward|ffn"
    unless_equals: [d_model]
    sym: "2*d_ff"
    name: "2·d_ff (dense FFN gate+up 융합 투영 폭)"
