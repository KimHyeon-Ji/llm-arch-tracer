# 가중치 없이 meta/fake device로만 forward를 돌리므로 GPU/CUDA가 필요 없다.
# torch를 CPU 전용 wheel로 설치하려면:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
# (그냥 pip install -r requirements.txt 만 해도 동작은 하지만, CUDA 빌드가 잡히면
#  다운로드 용량이 훨씬 커진다 — 이 프로젝트엔 GPU 연산이 전혀 없으므로 CPU 빌드로 충분하다)

torch==2.13.0
transformers==5.14.1
huggingface_hub==1.24.0
PyYAML==6.0.3
numpy==2.5.1
