# 신규/미지 모듈 대응 절차

이 문서에는 **서로 다른 두 개의 진입점**이 있다. 둘 다 반드시 돈다.

| | 언제 | 무엇을 | 어디에 |
|---|---|---|---|
| **Phase 0 — 신규 모델 온보딩** | `model_type`이 처음이거나 config에 모르는 필드가 있을 때. **실패하지 않아도 무조건** | 이 모델이 새로 들고 온 **구조**를 찾아 이해하고 등록 | `rules/symbols.yaml`, `rules/derived_dims.yaml`, `rules/structures/` |
| **Tier 0~3 — 실패 대응** | 트레이싱이 깨지거나 규칙표에 없는 op이 나왔을 때 | 그 **오류/라벨** 하나를 푼다 | `rules/error_remedies.yaml`, `rules/optype_map.yaml` |

> **왜 둘로 나눴나 (2026-07-27, DeepSeek-V4에서 실제로 당한 실패).**
> 예전 이 문서는 "트레이싱이 실패하거나 규칙표에 없는 op이 나왔을 때의 절차"로만 정의돼 있었다.
> DeepSeek-V4-Pro는 C1~C16을 **전부 PASS**했다. 그래서 Tier 0~3 중 **어느 것도 발동하지 않았고**,
> CSA·HCA·Lightning Indexer·mHC·grouped output projection이라는 **완전히 새로운 모듈 5종이
> 통째로 문서화되지 않은 채** 통과했다. `model_summary.md`에는 정체불명 상수 7개가 남았고,
> attention은 `MQA` 한 단어로만 적혔다. 실패 기반 절차는 "조용히 잘못된 산출물"을 못 잡는다.
> **성공했어도 새 구조면 반드시 조사한다** — 그게 Phase 0이다.

```
Phase 0  신규 모델 온보딩 (실패 여부와 무관, 새 아키텍처면 항상)
   A. 문서로 구조 파악  → B. 코드로 확인  → C. 트레이스로 검증  → D. 규칙에 등록
   ↓ (등록 못 한 게 남으면)
Tier 3  사람 검증 요청

Tier 0  환경/접근 오류 배제 → 모델링과 무관한 문제면 여기서 바로 해결
   ↓
Tier 1  알려진 오류 패턴 → 조치 자동 적용 → 재시도
   ↓ (미분류 오류로 실패)
Tier 2  공식 소스 리서치 → 라벨/오류 해석만 보강 → 재시도
   ↓ (여전히 실패하거나 라벨 미확정)
Tier 3  사람 검증 요청 → 답변을 규칙표에 영구 반영
```

## 변경 후 필수 검증 (모든 작업의 마지막 단계)

`rules/*.yaml` 또는 `src/*.py`를 고쳤으면 **반드시** 아래를 돌린다. 통과(FAIL 0) 전까지
"고쳤다"고 말하지 않는다.

```bash
.venv\Scripts\python.exe develop\regen_summaries.py     # 재추적 없이 산출물 갱신
.venv\Scripts\python.exe develop\verify_all.py          # FAIL 0 이어야 함 (종료코드 0)
```

`verify_all.py`가 검사하는 것:

| 단계 | 내용 |
|---|---|
| STATIC | `rules/*.yaml` 파싱 + **중복 키**, `src/*.py` import, symbols/derived_dims 스키마·정규식·priority 중복 |
| FLEET | 모델별 C체크 FAIL, C17, 미해결 유도 상수, bare 정수 수/비율, 미확인 심볼 |
| EXTERNAL | KV cache 카드 vs 공개 갤러리 수치(`develop/verify/references.yaml`) |
| BASELINE | `develop/verify/baseline.json` 대비 **퇴행이면 실패**(개선은 통과시키되 기준 갱신 안내) |

기준을 올릴 땐 결과를 **검토한 뒤** `--update-baseline`.

### 왜 이 게이트를 만들었나 (2026-07-27 실제 사고 기록)
아래는 전부 "고쳤다"고 말한 뒤에 뒤늦게 발견된 것들이다. 공통 원인은 하나 —
**주장에 자동 확인이 없었다.**

| 사고 | 무엇이 잡았어야 했나 |
|---|---|
| PowerShell 정규식으로 `symbols.yaml`을 고치다 심볼명이 `d_moe`→`d_moE`로 깨지고 `scope:` 키가 중복됨. `yaml.safe_load`는 중복 키를 조용히 마지막 값으로 덮어써서 무증상 통과 | STATIC 중복 키 검사 |
| docstring 편집이 `summarize.py`를 SyntaxError로 만듦 — 재생성 돌릴 때까지 몰랐음 | STATIC import 검사 |
| 심볼라이저를 "scope 밖 심볼은 버린다"로 바꿨더니 정상 심볼 **88,000개**가 bare 정수로 퇴행. 우연히 감사를 다시 돌려 발견 | BASELINE 퇴행 검사 |
| 감사 스크립트 자체가 틀림(수식 `2*d_head` 안의 계수 `2`까지 셈) → 잘못된 결론 보고 | 감사 로직을 레포에 고정(스크래치 스크립트 금지) |
| CSV 첫 컬럼 `op_id`를 shape 값으로 오인해 grep | shape 필드만 파싱하는 고정 스캐너 |
| `2*T` 수정이 기존 산출물에 반영됐다고 말했으나 실제로는 재트레이싱 전이라 미반영 | FLEET 지표(주장이 아니라 측정값) |

**규칙**: 측정은 일회용 스크립트로 하지 않는다. 측정 로직이 틀리면 결론이 통째로 틀리므로,
검증 코드도 산출물과 같은 수준으로 레포에 고정하고 리뷰 대상으로 둔다.

## 마지막 관문 — 자유 평가 (규칙이 못 잡는 것)

`verify_all.py`는 **규칙 기반**이라 이미 겪은 오류만 인코딩한다. 본 적 없는 라벨 오류는
원리적으로 못 잡는다. 실제로 이 프로젝트에서 나온 심각한 오류는 **전부** 자유 평가에서
발견됐고 규칙 게이트는 하나도 못 잡았다:

| 발견된 오류 | 발견 경로 |
|---|---|
| Falcon KV cache 71배 과대평가(MQA를 MHA로) | 블라인드 테스트 |
| DeepSeek-V4 가중치가 시퀀스 길이에 의존(`T/m_csa`) | 외부 LLM 리뷰 |
| Llama-4 `E_shared=0`인데 트레이스엔 shared expert 120개 | 외부 LLM 리뷰 |
| gpt-oss `2*d_moe`가 `2*d_ff`로 오라벨 | 외부 LLM 리뷰 |
| xLSTM(attention 없음)이 MHA + 512 KiB KV로 분류 | 적대적 자체 감사 |
| Falcon `73`(= n_h+2·n_kv)이 정수로 방치 | 리뷰 패킷을 훑다가 |

그래서 **규칙 통과 = 끝이 아니다.** 마지막에 자유 평가를 한 번 돌린다.

### 리뷰 패킷을 쓴다 (그냥 "봐달라"고 하지 않는다)

```bash
.venv\Scripts\python.exe develop\make_review_packet.py <model-dir>      # 단일
.venv\Scripts\python.exe develop\make_review_packet.py --all            # 전체
```

패킷은 **자기완결적**이다 — 요약·심볼표·검증결과·대표 트레이스 표본(모듈×op 조합마다
1행)·이미 알려진 한계가 모두 들어간다. 리뷰어가 저장소를 뒤지게 하지 않는다.

### 리뷰어에게 요구하는 것

패킷이 출력 형식을 강제한다. 핵심은 **관찰과 가설의 분리**다.

| 요구 | 이유 |
|---|---|
| 관찰(사실)과 가설(원인)을 다른 칸에 | 2026-07-29 외부 리뷰는 **관찰은 대체로 옳았지만 근본 원인 진단은 절반이 틀렸다.** 섞여 있으면 옳은 관찰까지 버리게 된다 |
| 근거는 파일명+함수명+인용문 또는 URL | 추적 가능해야 재현할 수 있다 |
| **`op_id`는 근거로 금지** | 재트레이싱하면 번호가 전부 어긋난다. 실제로 그래서 인용된 op_id가 하나도 안 맞았다. `module_path`+shape 내용으로 지목할 것 |
| 확신도 표기(확실/아마도/추측) | 검증 순서를 정하는 데 필요 |
| 이미 알려진 한계는 재보고 금지 | 패킷에 실어 보낸다 |

### 받은 지적을 처리하는 규칙 — **하나도 그대로 믿지 않는다**

1. **모든 주장을 먼저 재현한다.** 현재 코드·config·트레이스로 직접 확인하기 전에는
   아무것도 고치지 않는다. 지적의 절반은 원인 진단이 틀렸고, 인용 위치는 낡아 있었다.
2. **관찰이 맞고 원인이 틀린 경우가 가장 흔하다.** "여기 이상하다"는 신호로만 받고
   원인은 직접 판정한다.
3. 고친 뒤에는 반드시 「변경 후 필수 검증」을 돌려 **FAIL 0**을 확인한다.
4. 새로 드러난 오류 유형은 **가능하면 규칙으로 승격**한다. 자유 평가는 매번 돌릴 수
   없으므로, 한 번 잡힌 유형은 다음부터 자동으로 걸려야 한다.
5. **승격한 검사에는 반드시 주입 케이스를 같이 넣는다**(`develop/verify_selftest.py`).
   주입 없이 추가한 검사는 "0건"이 결함이 없어서인지 검사가 안 도는 건지 구분이 안 된다.
   실제로 attention 레이어 수 검사가 죽은 채로 falcon(32→0)을 통과시킨 적이 있다.
6. `T`가 **선택되는 방식**을 고쳤다면 재생성으로는 반영되지 않는다 — 재추적해야 한다
   (`provenance.seq_len_used` vs 지금 `resolve_seq_len`이 고를 값을 비교해 확인).

> **자유 평가는 규칙을 대체하지 않는다 — 규칙을 만들어내는 공급원이다.**
> 지금 게이트에 있는 라벨 검사(가중치 T 불변식, 데이터플로우 일관성, 산술적 참,
> 파라미터 라벨 일관성, n_h/n_kv 상호배제, 잔차 LayerNorm 폭)는 전부 이 경로로
> 발견된 뒤 승격된 것들이다.

### 가장 잘 반복되는 라벨 오류 — **스코프 과매칭**

지금까지 승격된 오류의 다수가 한 가지 원인이다: `scope` 정규식이 **모듈 이름의 부분
문자열**만 보기 때문에, 이름이 위치를 뜻할 뿐인 모듈까지 걸린다.

| 모듈 | 걸린 스코프 | 실제 정체 |
|---|---|---|
| `post_attention_layernorm` | `attn\|attention` | attention이 **아니라** 그 뒤의 잔차 정규화 → `d_model` |
| `mlp.router`, `mlp.gate` | `mlp`(d_ff) | FFN 내부가 아니라 **잔차를 읽는 라우터** → `d_model` |
| `mlp.experts` | `mlp`(d_ff) | dense FFN이 아니라 expert → `d_moe` |
| `compressor.gate_proj` | `gate`(E/k) | MoE 라우터가 아니라 평범한 SwiGLU 투영 |
| `compressor.indexer` | `compressor`(m_hca) | 압축기 본체와 어휘가 다름 → `c_I` |

새 스코프를 쓸 때 확인할 것:
1. **그 이름이 "무엇을 계산하는가"인가, "블록 안 어디인가"인가?** 후자면 스코프에 넣지 않는다.
2. 음의 전방탐색으로 하위 모듈을 **명시적으로 제외**한다(`(?!.*expert)`, `(?!.*router)`).
3. 값이 같은 심볼이 여럿일 때는 **`priority`로 특화 이름을 앞에** 둔다.
4. 다른 데서는 의미가 없는 국소 이름이면 **`scope_strict: true`** — 스코프 밖에서는 강등이
   아니라 후보에서 아예 뺀다. (기본은 강등이다. 전면 드롭은 2026-07-29에 88k 축을 정수로
   되돌린 전력이 있어서 심볼별 옵트인으로만 허용한다.)

**교훈**: 값이 우연히 같은 모델에서만 드러나므로, 스코프 과매칭은 대개 **잠복**한다.
`d_ff` 과매칭은 `hidden_size == intermediate_size`인 gpt-oss에서야 보였고, 잔차폭 과매칭은
`d_model == n_h·d_head`인 Llama-70B/405B에서야 보였다. 그래서 "지금 결과가 맞다"가 아니라
**"이 스코프가 의미상 맞나"**로 판단해야 한다.

### 두 번째로 잘 반복되는 오류 — **휴리스틱이 지어낸 이름**

`symbolic_shape.dim()`은 등록된 규칙을 다 못 맞히면 마지막에 휴리스틱으로 이름을 만든다
(`sym+1`, `c*sym`, `T*sym`, `sym/2`). 값만 맞추면 되므로 **거의 항상 뭔가는 만들어낸다** —
그리고 그건 산술적으로 참이라 게이트를 그대로 통과한다.

2026-07-31 자유 평가에서 이 경로로 만들어진 거짓 이름이 12개 모델에서 나왔다. 전부 RoPE의
`rotate_half` 분할 축(`d_head/2` 또는 `d_rope/2`)인데 `2*n_h`·`4*n_kv`·`4*k`·`4*E`·`n_h/2`로
찍혀 있었다. `k`는 expert top-k, `E`는 expert 수다 — self_attn 안에서 나올 이유가 없다.

그래서 두 가지를 지킨다:

1. **휴리스틱은 in-scope/unscoped 심볼만 쓴다.** 스코프가 명시적으로 배제한 심볼로 *식을
   지어내는* 것은 그 심볼을 그대로 붙이는 것보다도 약한 근거다.
2. **진짜 축은 출처와 함께 `derived_dims.yaml`에 등재한다.** 그러면 tier 2(in-scope 파생)에서
   결정되어 휴리스틱까지 내려가지 않는다.

**정수로 남는 걸 두려워하지 마라.** 거짓 이름보다 정직한 정수가 낫다. 다만 남은 정수는
**값마다 기약인지 확인해서** `develop/verify/references.yaml`의 `irreducible_literals`에
근거와 함께 적는다. 설명 안 된 상수는 `unresolved`/C17이 따로 잡는다.

> ⚠️ `bare`(정수로 남은 축 수)는 "낮을수록 좋다"로 읽히지만 **거짓 이름을 붙이면 낮아진다.**
> 이 지표가 늘었다는 건 나쁜 신호가 아니라 **확인이 필요하다는 신호**다.

### 규칙을 추가할 때 흔한 함정

- **`sym`에 `^`를 쓰지 마라.** 렌더된 라벨은 검증에서 그대로 `eval`되는데 Python `^`는
  거듭제곱이 아니라 XOR이다. `n_hc^2`로 뒀다가 산술 검사에서 588건이 거짓으로 잡혔다.
- **심볼을 추가했으면 재생성만으로 부족한지 확인하라.** `provenance.symbol_table`이 갱신되지
  않으면 산술 검사가 그 심볼을 못 읽어 해당 라벨을 **조용히 건너뛴다**(2026-07-31에 xLSTM
  라벨 70,560개가 그 상태였다). 지금은 `regen_summaries.py`가 표를 갱신한다.
- **이미 쓴 문서를 먼저 봐라.** V4의 mHC 폭은 `rules/structures/residual/mhc.md`에 정답이
  적혀 있었는데도 심볼라이저가 그걸 안 쓰고 있었다. 구조 문서와 규칙이 어긋나면 규칙이 틀린 쪽이다.

## Phase 0 — 신규 모델 온보딩 (문서 먼저, 코드 나중)

**발동 조건** (하나라도 해당하면 실행):
- `config.model_type`이 `models/`에 없던 값이다
- config에 `rules/symbols.yaml` 별칭으로 안 잡히는 필드가 있다
- 생성된 `model_summary.md`에 **미해결 유도 상수**가 남았다 (⚠ 섹션이 찍힌다)
- C16 unmapped op에 처음 보는 raw op이 있다

**순서가 중요하다.** 코드부터 읽으면 "이 프로젝션이 왜 여기 있는지" 모르는 채로 shape만 베끼게 된다.
설계 의도를 먼저 알고 코드를 보면 같은 코드가 훨씬 빨리 읽힌다. 반대로 코드에서 처음 보는 게
나오면 다시 A로 돌아간다 — 한 방향이 아니라 **왕복**이다.

### A. 문서로 먼저 — "이 모델이 새로 들고 온 게 뭔가"
코드를 열기 전에 아래를 읽고 **새 모듈 목록과 각각의 역할·차원 이름**을 가설로 적는다.

| 순위 | 소스 | 여기서 얻을 것 |
|------|------|----------------|
| 1 | 공식 model card / 저장소 README | 새 모듈의 **이름**과 한 줄 설명 |
| 2 | 공식 기술 리포트·논문(모델 카드 링크) | 수식·기호·절 번호. `rules/structures/` 문서의 "정의"가 여기서 나온다 |
| 3 | 벤더 공식 블로그 | 설계 동기(왜 이 구조를 넣었나) |
| 4 | **독립 서빙 프레임워크의 릴리스 블로그·문서** (vLLM / SGLang / TensorRT-LLM) | 구현자 관점의 재서술 + **실측 수치**. HF 구현이 모호할 때 특히 강력하다 |
| 5 | [Raschka's LLM Architecture Gallery](https://sebastianraschka.com/llm-architecture-gallery/) | 계보 비교("무엇의 변형인가") + 카드 수치(KV cache/token 등)와 [계산 규약](https://sebastianraschka.com/llm-architecture-gallery/kv-cache-calculations/) |
| 6 | 커뮤니티 개요 — [PyTorchKR 읽을거리](https://discuss.pytorch.kr/c/news/14), HF 블로그·커뮤니티 글 | 오리엔테이션용. 2차 자료라 **반드시 1~5로 재확인**하고, 인용 시 출처를 2차로 명시 |

산출물: "새 모듈 가설 목록". 예 — DeepSeek-V4에서는 `CSA(m=4)`, `HCA(m′=128)`, `Lightning Indexer`,
`mHC(n_hc 스트림 + Sinkhorn)`, `grouped output projection(g, d_g)`, `attention sink`, `Hash-MoE`, `MTP`.

### B. 코드로 확인 — 가설을 맞춰본다 (1차 소스)
1. **지금 실행되는 modeling 소스**의 클래스·docstring·변수명 (`.venv/.../transformers/models/<type>/`)
2. 같은 저장소 **config 클래스 docstring** — 필드 ↔ 논문 기호 대응이 여기 적혀 있는 경우가 많다
3. **독립 구현 소스** (vLLM/SGLang/TRT-LLM의 해당 모델 파일)

A와 B가 어긋나면 **B가 이긴다**. 우리가 트레이싱하는 건 실제로 실행되는 그 코드다(P1).
단, 문서에만 있고 코드에 없는 모듈은 "코드에 없음"을 사실로 기록한다(예: native impl의 MTP 누락 → C15 WARN).

### C. 트레이스로 검증 — 숫자가 맞는지
가설한 차원 공식을 **실제 트레이스 값으로 재현**해본다. 재현되면 확정, 안 되면 A/B로 복귀.
가능하면 **외부에 공개된 수치와도 대조**한다(갤러리 카드, 벤더/서빙 프레임워크 블로그의 KV cache·활성 파라미터 등).
교차 일치하면 그 자체가 강한 검증이다.

### D. 등록 — 산출물에 반영 (여기까지 해야 조사 완료)
| 알아낸 것 | 등록할 곳 |
|---|---|
| config 필드 ↔ 심볼 1:1 | `rules/symbols.yaml` (`aliases`, `group`, `dim`, `priority`) |
| 계산으로 나오는 합성 차원 | `rules/derived_dims.yaml` (`expr` + `name` + **출처 주석**) |
| 구조적 패턴(attention/MoE/정규화/위치인코딩/잔차/보조/SSM…) | `rules/structures/<category>/<name>.md` (형식은 그 폴더 README) |
| 처음 보는 raw op의 라벨 | `rules/optype_map.yaml` |
| 새 오류 패턴과 조치 | `rules/error_remedies.yaml` |

등록 후 재생성해서 **미해결 유도 상수 0**을 확인한다.

### 조사 완료 판정 (Definition of Done)
아래를 다 만족해야 그 모델은 "온보딩 완료"다.
- [ ] `model_summary.md`에 **⚠ 미해결 유도 상수 섹션이 없다**
- [ ] 심볼 표의 `미확인`이 전부 설명된다(진짜 없는 필드면 `group`으로 "해당 없음" 처리)
- [ ] 새 구조가 `rules/structures/`에 문서화되고 "확인된 모델" 목록에 이 모델이 있다
- [ ] 핵심 수치 최소 1개가 **독립 소스와 교차 일치**한다
- [ ] 남은 불확실은 Tier 3 패킷으로 사람에게 넘겼다
- [ ] **`develop/verify_all.py`가 FAIL 0으로 통과한다** (위 「변경 후 필수 검증」)

### 사람에게 넘기는 기준 (→ Tier 3)
아래는 **추측해서 채우지 말고** 반드시 사람에게 확인을 요청한다(P1).
- A~C를 다 돌았는데 유도 상수의 유래를 못 찾음
- 소스마다 설명이 엇갈리고 코드로도 판별이 안 됨
- 값은 재현되는데 **여러 해석이 동시에 성립**한다(예: 한 정수가 서로 다른 두 양과 값이 같음)
- 공개 수치와 우리 계산이 어긋나고 원인을 못 찾음

질문은 자유 서술이 아니라 **선택형**으로 만든다(아래 Tier 3 패킷 형식).

## Tier 0 — 환경/접근 오류 배제

새 모델을 처음 만났을 때 나는 오류의 상당수는 모델링과 무관한 환경 문제다. 이걸 모델링 이슈로 잘못 분류해 Tier 1~3을 헤매지 않도록 가장 먼저 배제한다.

| 증상 | 원인 | 조치 |
|------|------|------|
| 401 / 403, gated repo 관련 | HF 라이선스 미동의 또는 토큰 미인증 | `huggingface-cli login` 후 모델 페이지에서 라이선스 동의, 재시도 |
| `KeyError: '<model_type>'` | 설치된 transformers가 이 아키텍처를 모름 | 라이브러리 업그레이드, 01-main.md Step 1 지원 게이트로 재확인 |
| revision/파일을 찾을 수 없음 | commit hash 오타, 브랜치명 착오 | revision 재확인 |
| 알 수 없는 import/버전 충돌 | torch·transformers 버전 조합 문제 | 두 라이브러리 동시 업그레이드 |

## 실패 원인 좁히기 — 변수 격리

모델이 복잡할수록 "무엇 때문에 깨졌는지"가 안 보인다. Tier 1 표를 뒤지기 전에, canary suite(`01-main.md` §9.1, `develop/canary/suite.yaml`)에 이미 통과한 모델들이 각각 무엇을 검증했는지(태그: GQA, MoE-simple, MoE+MLA 조합, 이종 attention, MTP 등)를 먼저 확인한다.

- 실패한 모델의 구성요소 중 **이미 다른 canary에서 개별 검증된 것**과 **이번에 처음 등장한 것**을 구분한다.
- 의심 범위를 처음 등장한 요소로 좁혀서 Tier 1/2를 적용한다.
- 두 요소가 상호작용해서 깨진 것 같고 어느 쪽인지 안 보이면, 가능한 경우 config를 줄여(레이어 수 축소, 한쪽 기능 비활성화) 요소 하나만 살아있는 축소 재현을 먼저 만들어본다.

## Tier 1 — 알려진 오류 패턴

| 오류 패턴 | 원인 | 조치 |
|-----------|------|------|
| `NotImplementedError`, meta에서 실행 불가 | meta 커널 부재 | FakeTensorMode로 승격 |
| `Boolean value of Tensor ...` | tensor 값으로 분기하는 제어흐름 | `attn_implementation="sdpa"`로 강제 |
| data-dependent 관련 오류 | 동적 제어흐름 | `attn_implementation="eager"`로 전환 |
| topk/index 관련 "out of range" | seq_len이 config의 top-k·window보다 작음 | seq_len을 2배로 올려 재시도 |
| cache/Cache 관련 오류 | cache 형태가 모델 기대와 다름 | 모델이 제공하는 cache 생성 유틸로 재구성 |

## Tier 2 — 공식 소스 리서치

Tier 1로 해결 안 되면, 짐작하기 전에 아래 순서(신뢰도 내림차순)로 확인한다. 상위에서 답이 나오면 하위는 생략한다.

| 순위 | 소스 |
|------|------|
| 1 | 지금 실행 중인 modeling 소스의 주석·변수명·docstring |
| 2 | 같은 저장소 config 클래스의 docstring |
| 3 | **독립 서빙 구현 — vLLM / SGLang / TensorRT-LLM의 해당 모델 소스**: HF 구현이 모호하거나 갓 나온 모델이라 설명이 부족할 때, 같은 모델을 별도로 재구현한 실행 가능한 코드라서 op 구조·이름 관례를 교차검증하는 데 특히 유용하다 |
| 4 | 저장소 README / 공식 model card |
| 5 | 라이브러리에 병합된 아키텍처라면 공식 문서 페이지 |
| 6 | model card에 링크된 논문/기술 리포트 |
| 7 | 아키텍처 비교/갤러리 자료(예: [Sebastian Raschka's LLM Architecture Gallery](https://sebastianraschka.com/llm-architecture-gallery/)) — 여러 모델의 아키텍처를 한곳에 정리한 2차 자료. HF와 나란히 확인하면 특히 attention·MoE·잔차 연결 등 구조 변형의 명칭·계보를 빠르게 파악하는 데 유용하다. 원 소스(모델 카드·논문)로 반드시 재확인할 것 |
| 8 | 공개 벤치마크 자료(활성 파라미터 수·처리량 등 — op 의미보다는 `model_summary.md`의 정합성 참고용) |
| 9 | 커뮤니티 개요 — [PyTorchKR 읽을거리&정보공유](https://discuss.pytorch.kr/c/news/14), HF 블로그·커뮤니티 글. 신규 모델의 **발표·개요 파악**에는 빠르지만 아키텍처 심층 내용은 얕다. 2차 자료이므로 1~7로 재확인하고 인용 시 "2차" 표기 |
| 10 | 일반 웹 검색(비공식, 최후 수단 — 사용 시 반드시 "비공식" 표기) |

**규칙**: 리서치 결과는 op_type 라벨과 오류 원인 해석, 그리고 `model_summary.md`(`01-main.md` §11)의 참고 소스 절에만 반영한다. **이미 dispatch로 확정된 shape·dependency 값은 절대 덮어쓰지 않는다.** 어떤 소스로도 확인이 안 되면 라벨을 임의로 붙이지 말고 `unmapped` 상태로 Tier 3에 넘긴다. 이 단계는 실행 주체가 코딩 에이전트임을 전제로 자동화되며, 순수 배치 스크립트 환경에서는 사람이 대신 수행하고 결과만 반영한다.

## Tier 3 — 사람 검증 요청

자유 서술형 질문이 아니라, 최소 재현 정보와 선택형 질문으로 구성한다.

요청 패킷에 포함할 것:
- 실패한 op_id / module_path / raw op
- 관련된 depends_on 서브그래프(최소 재현에 필요한 범위만)
- 관련된 config 필드만 추출한 값
- Tier 0/1/2에서 이미 시도하고 배제한 것들, 그리고 변수 격리로 좁힌 범위
- Tier 2에서 찾은 후보 해석(있다면) 및 그 출처
- 선택형 질문 예: "이 op은 (A) 결정론적 구조 연산인가 (B) 데이터 의존 라우팅/제어흐름인가?"

Phase 0에서 넘어온 경우에는 위에 더해 **A~C에서 확인된 것과 확인 안 된 것을 구분해** 적는다
(어떤 소스를 봤고 무엇이 안 나왔는지). 사람이 같은 검색을 반복하지 않게 하는 게 목적이다.

사람이 답하면:
1. `provenance.json`에 `source=human`으로 근거를 남긴다.
2. 정규화 규칙표(`rules/optype_map.yaml`) 또는 Tier 1 오류 패턴표(`rules/error_remedies.yaml`)에 영구 반영한다. 이후 같은 패턴이 다른 모델에서 나오면 자동으로 처리된다.
3. 확인된 게 개별 op 하나가 아니라 **attention, MoE/FFN, 정규화, 위치 인코딩, 잔차 연결, 보조 모듈 같은 구조적 패턴**(예: 새로운 압축 attention 변형, 새로운 라우팅 방식, 새로운 잔차 연결 변형)이면, `rules/structures/`(구조 라이브러리, `01-main.md` §12)에도 항목을 추가하거나 기존 항목의 "확인된 모델" 목록을 갱신한다.

## 자주 나오는 신규 구조 범주 (참고용)

특정 모델을 지칭하지 않는 일반적 패턴이다.

| 범주 | 특징 | 흔한 실패 지점 |
|------|------|----------------|
| 동적 압축/희소 선택 모듈 | 고정 top-k나 압축률로 일부 위치만 선택 | seq_len이 top-k/압축률보다 작으면 즉시 에러 → Tier 1 seq_len 상향 |
| 비표준 잔차 연결 | 단일 elementwise add가 아닌 다중 스트림 믹싱 | 검증에서 "두 피연산자 shape 동일" 같은 단순 규칙이 안 맞음 → 완화된 불변식 필요(`01-main.md` §9.2 C5 참고) |
| 값 기반 정적/결정적 라우팅 | 토큰 ID 등 입력 값에 따라 결정되는 라우팅 | 0으로 채운 입력에서는 라우팅이 왜곡됨 → arange 입력으로 해결(오히려 정확히 캡처됨) |
| 메인 forward 밖의 보조 모듈 | 추론 시 별도로 호출되는 보조 예측/보조 헤드 등 | 커버리지 검사가 누락을 잡아주지만, 자동으로 트레이싱되진 않음 → 별도 진입점으로 명시 필요 |
| 레이어별로 다른 attention 종류(로컬/글로벌 혼합 등) | 레이어마다 op 시퀀스·mask 범위가 다름 | 동질성 가정이 깨짐 → C2를 클러스터링으로 검증, 해당 스케줄에서는 축소 최적화 금지 |
| logit/score 스케일링·클리핑(soft-cap 등) | 지금까지 못 본 elementwise 연산(예: tanh 기반 스케일링) | 정규화 규칙표에 없으면 `unmapped`로 표시 후 Tier 2로 |

## 구조적 한계 (해결 불가 카테고리)

아래는 조치를 아무리 정교하게 짜도 근본적으로 해결되지 않는 경우다. 발생 시 표에 기록하고 사람에게 "이 op은 방법론적으로 트레이싱 불가"로 명확히 보고한다.

- 공식 코드가 하드웨어 전용 커널을 eager 대체 경로 없이 직접 호출하는 경우.
- 텐서의 실제 값에 의존하는 Python 제어흐름(`.item()` 결과로 분기 등). meta/fake 실행 자체의 한계이며 트레이서의 버그가 아니다.
