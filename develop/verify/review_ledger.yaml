note: ③ 자유 평가(README) 수행 기록. fingerprint 는 검토 대상 산출물의 해시이고, 지금 산출물과 다르면 그 검토는 만료다.
models:
  HuggingFaceTB__SmolLM3-3B-Base:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: dbbe3cdd53e1d11c
    findings: 0
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  NX-AI__xLSTM-7b:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: 0bd4686e4b27cce1
    findings: 4
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  Qwen__Qwen2.5-0.5B:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: 4f6380fb6a88857a
    findings: 2
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  Qwen__Qwen3-30B-A3B:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: 339e27689fe048e5
    findings: 2
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  Qwen__Qwen3-Next-80B-A3B-Instruct:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 자기모순 추적 + 소스 대조)
    fingerprint: cb40313cb6729527
    findings: 11
    notes: 게이트가 센 자기모순(같은 텐서를 한 행/한 엣지 안에서 두 이름으로 부르는 곳)을 출발점으로 linear_attn 전건 추적.
      1·2회차 판정 유지. C절(모듈별 출력 shape) 미수행.
  Zyphra__Zamba2-1.2B:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: 270b230f608d6ab0
    findings: 6
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  allenai__OLMo-2-1124-7B-Instruct:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: f79f29d2f4b409fa
    findings: 0
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  allenai__OLMoE-1B-7B-0924:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: d5be808e150b8058
    findings: 1
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  bzantium__tiny-deepseek-v3:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: e490f7803605acae
    findings: 2
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  deepseek-ai__DeepSeek-V2-Lite:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: 781572e335f25f78
    findings: 2
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  deepseek-ai__DeepSeek-V3:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: d09fa953581f1a8e
    findings: 1
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  deepseek-ai__DeepSeek-V4-Flash:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: 7492d7a950248d76
    findings: 7
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  deepseek-ai__DeepSeek-V4-Pro:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: c28fdaf171e3178c
    findings: 6
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  google__gemma-2-2b:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: 36cb7eff2d188854
    findings: 0
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  google__gemma-3-270m:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: 50a8b1e1e5ba587a
    findings: 0
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  hf-internal-testing__tiny-random-LlamaForCausalLM:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: 8bd9d13d1a4edd11
    findings: 0
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  meta-llama__Llama-3.1-405B:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: fc443f7c71499258
    findings: 0
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  meta-llama__Llama-3.1-70B:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: 45698b627a287e17
    findings: 0
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  meta-llama__Llama-3.1-8B:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: 60ad5829f83244e0
    findings: 0
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  meta-llama__Llama-4-Maverick-17B-128E:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: 084e8d0c842a98c2
    findings: 2
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  nvidia__NVIDIA-Nemotron-3-Nano-4B-BF16:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: 7de578db178f2ef6
    findings: 4
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  openai-community__gpt2-xl:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: c5acb8e03e8e90be
    findings: 1
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  openai__gpt-oss-120b:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: faac602bd23ba8a3
    findings: 1
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  openai__gpt-oss-20b:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: 43da063169646046
    findings: 1
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  tiiuae__falcon-7b:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: 3e7a5e845efbdcd0
    findings: 2
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  zai-org__GLM-4.5-Air:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: 8d5996ef22e2d677
    findings: 2
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  Qwen__Qwen3.5-4B:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 자기모순 추적 + 소스 대조)
    fingerprint: 44bd42c63906d71d
    findings: 7
    notes: 게이트가 센 자기모순(같은 텐서를 한 행/한 엣지 안에서 두 이름으로 부르는 곳)을 출발점으로 linear_attn 전건 추적.
      1·2회차 판정 유지. C절(모듈별 출력 shape) 미수행.
  Qwen__Qwen3.6-27B:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 자기모순 추적 + 소스 대조)
    fingerprint: 62fe5b9d039c0e62
    findings: 7
    notes: 게이트가 센 자기모순(같은 텐서를 한 행/한 엣지 안에서 두 이름으로 부르는 곳)을 출발점으로 linear_attn 전건 추적.
      1·2회차 판정 유지. C절(모듈별 출력 shape) 미수행.
  Qwen__Qwen3.6-35B-A3B:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 자기모순 추적 + 소스 대조)
    fingerprint: b4ab614e4f9ba086
    findings: 7
    notes: 게이트가 센 자기모순(같은 텐서를 한 행/한 엣지 안에서 두 이름으로 부르는 곳)을 출발점으로 linear_attn 전건 추적.
      1·2회차 판정 유지. C절(모듈별 출력 shape) 미수행.
  moonshotai__Kimi-K2-Instruct:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: a8a2776288abd199
    findings: 1
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  moonshotai__Kimi-K2.6:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: 2bc4710e482668f9
    findings: 1
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  deepseek-ai__DeepSeek-V4-Flash-0731:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: e28ca550dbcf611d
    findings: 8
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  moonshotai__Kimi-K2.7-Code:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: d1c8d73cae808bc2
    findings: 1
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  nvidia__NVIDIA-Nemotron-3-Super-120B-A12B-BF16:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: 1b9c9ff448913b43
    findings: 4
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  nvidia__NVIDIA-Nemotron-3-Ultra-550B-A55B-BF16:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: f2c85f8a66bf9e62
    findings: 3
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
  zai-org__GLM-5.2:
    reviewed_on: '2026-08-11'
    reviewer: llm(claude, 전수 점검 2회차 — 모듈-필드 소속)
    fingerprint: d6d4150df9b4b8bd
    findings: 2
    notes: '전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을
      함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히
      미수행.'
