note: ③ 자유 평가(README) 수행 기록. fingerprint 는 검토 대상 산출물의 해시이고, 지금 산출물과 다르면 그 검토는 만료다.
models:
  HuggingFaceTB__SmolLM3-3B-Base:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: c6645fb22db07a79
    findings: 0
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  NX-AI__xLSTM-7b:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: 1af2b098f730bd2f
    findings: 5
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  Qwen__Qwen2.5-0.5B:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: 76d19d2279dd0f54
    findings: 2
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  Qwen__Qwen3-30B-A3B:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: f1a41d7748af7092
    findings: 2
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  Qwen__Qwen3-Next-80B-A3B-Instruct:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: bd9e56380a192676
    findings: 13
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  Zyphra__Zamba2-1.2B:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: abcf8a3ca74fb087
    findings: 6
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  allenai__OLMo-2-1124-7B-Instruct:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: b7f3cd029bb8414b
    findings: 2
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  allenai__OLMoE-1B-7B-0924:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: 9bcd02777ad209cc
    findings: 1
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  bzantium__tiny-deepseek-v3:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: 0b24bfe7c6946a7c
    findings: 8
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  deepseek-ai__DeepSeek-V2-Lite:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: f1af1b12f9a96619
    findings: 7
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  deepseek-ai__DeepSeek-V3:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: c7c6ea6a178f369d
    findings: 7
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  deepseek-ai__DeepSeek-V4-Flash:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: f4c926b294dd55b3
    findings: 12
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  deepseek-ai__DeepSeek-V4-Pro:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: ad9a3a3debe67202
    findings: 11
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  google__gemma-2-2b:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: 2fcd161815508bd9
    findings: 0
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  google__gemma-3-270m:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: 5dc37d1dda80e952
    findings: 0
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  hf-internal-testing__tiny-random-LlamaForCausalLM:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: 13331ae80340c94c
    findings: 1
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  meta-llama__Llama-3.1-405B:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: d4b07b2c63923e6f
    findings: 2
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  meta-llama__Llama-3.1-70B:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: b98364a2e5c4810a
    findings: 1
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  meta-llama__Llama-3.1-8B:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: e5b01849f58c3f8c
    findings: 1
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  meta-llama__Llama-4-Maverick-17B-128E:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: d0274f5e1f6672e9
    findings: 2
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  nvidia__NVIDIA-Nemotron-3-Nano-4B-BF16:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: e03ffaea74f771c2
    findings: 8
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  openai-community__gpt2-xl:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: fde7f691d9bae775
    findings: 1
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  openai__gpt-oss-120b:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: 80be67f7c6cd255b
    findings: 2
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  openai__gpt-oss-20b:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: d353e8983476970d
    findings: 2
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  tiiuae__falcon-7b:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: 8e0e6e7241ee544e
    findings: 2
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  zai-org__GLM-4.5-Air:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: cfd9f67e96fd5bcf
    findings: 2
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  Qwen__Qwen3.5-4B:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: 84ce4476bf4010ff
    findings: 7
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  Qwen__Qwen3.6-27B:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: e32e36ddc8ffbe21
    findings: 7
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  Qwen__Qwen3.6-35B-A3B:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: 40535358be9c908e
    findings: 7
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  moonshotai__Kimi-K2-Instruct:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: 83434328570b013f
    findings: 5
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  moonshotai__Kimi-K2.6:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: dea8889c62f5b207
    findings: 5
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  deepseek-ai__DeepSeek-V4-Flash-0731:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: 17bd8bc30a9790af
    findings: 13
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  moonshotai__Kimi-K2.7-Code:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: 949f325983757b9a
    findings: 5
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  nvidia__NVIDIA-Nemotron-3-Super-120B-A12B-BF16:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: e0f7b32af7968501
    findings: 9
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  nvidia__NVIDIA-Nemotron-3-Ultra-550B-A55B-BF16:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: 81c2f36a0462f080
    findings: 7
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  zai-org__GLM-5.2:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: 14110fd713b35f0e
    findings: 6
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  LiquidAI__LFM2-8B-A1B:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: d58d652c0499e190
    findings: 4
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  MiniMaxAI__MiniMax-M2:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: a4bda5d99149970d
    findings: 1
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  Qwen__Qwen3.5-397B-A17B:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: 6ce79d3ba88fe84b
    findings: 6
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  baidu__ERNIE-4.5-21B-A3B-PT:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: 23b6d0c0fcc5126d
    findings: 0
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  ibm-granite__granite-4.0-h-small:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: 476b5dc6a6cc6795
    findings: 3
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  microsoft__Phi-4:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: 65684217ddf1997c
    findings: 0
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  mistralai__Mistral-Small-3.2-24B-Instruct-2506:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: 79471661cfc850c5
    findings: 0
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
  tencent__Hunyuan-A13B-Instruct:
    reviewed_on: '2026-08-12'
    reviewer: llm(claude, 반박 프레임 전건 판정)
    fingerprint: a9b78eabb2f62f8e
    findings: 2
    notes: 의뢰서의 **모든** 질문에 답한다(기계가 개수를 맞춘다). 확인 프레임이 아니라 반박 프레임으로 — 각 라벨에 대해 '틀렸다는
      증거'를 먼저 찾고, 못 찾은 것만 맞다고 적었다. 외부 검토가 준 팁 3가지(op 내부 필드 상호 대조 / 요청·응답 개수 diff /
      반박 프레임)를 그대로 적용했다.
