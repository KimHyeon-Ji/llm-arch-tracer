# 외부 공개 수치 — 우리 계산이 독립 소스와 일치하는지 교차검증하는 기준값.
#
# 왜 파일로 두는가: 이 값들을 스크립트 안에 하드코딩해두면 "검증했다"는 주장만 남고
# 근거(출처·시점)가 사라진다. 여기 두면 verify_all.py가 매번 대조하고, 값이 바뀌면
# 출처를 다시 확인하게 된다.
#
# 주의: 이건 **라벨 교차검증용**이지 shape 값의 출처가 아니다(01-main.md P1).
# 우리 숫자는 전부 트레이스에서 나오고, 여기 값과 "우연히" 맞추려고 계산을 바꾸지 않는다.

kv_cache_per_token:
  source: "https://sebastianraschka.com/llm-architecture-gallery/"
  method: "https://sebastianraschka.com/llm-architecture-gallery/kv-cache-calculations/"
  checked_on: "2026-07-27"
  note: >
    BF16 2바이트. 표준 attention은 4·n_kv·d_head, K==V 통합이면 2·n_kv·d_head,
    MLA는 2·(kv_lora_rank + qk_rope_head_dim). 증가하는 캐시를 가진 레이어만 합산.
    밴드 경계(KiB): 24 / 72 / 160 / 300.
  # DeepSeek-V4 두 값(2026-09-01, Codex 외부검토 후 정정): 갤러리 방법론 페이지를 직접
  # 확인했더니 DeepSeek-V4/Lightning Indexer는 아예 다루지 않는다(DeepSeek-V3의 표준 MLA
  # 수식만 있음) -- 즉 갤러리 공개값은 인덱서의 자체 압축 key 캐시를 처음부터 계산에 넣을
  # 방법론이 없었다. 우리 값은 그 인덱서 캐시(+index_head_dim/m_csa 폭, 메인 압축기와 같은
  # amortised-growth 방식)를 포함하도록 트레이스 기반으로 고쳤다 -- 갤러리에 맞추려고 계산을
  # 바꾼 게 아니라, 갤러리가 모델링하지 않은 실제 성장 구조를 우리가 추가로 반영한 것이다.
  # 이전(인덱서 캐시 제외) 값은 V4-Flash "5.4 KiB (Very low)" / V4-Pro "7.7 KiB (Very low)".
  values:
    openai-community__gpt2-xl: "300.0 KiB (High)"
    allenai__OLMo-2-1124-7B-Instruct: "512.0 KiB (Very high)"
    meta-llama__Llama-3.1-8B: "128.0 KiB (Moderate)"
    meta-llama__Llama-4-Maverick-17B-128E: "192.0 KiB (High)"
    google__gemma-3-270m: "18.0 KiB (Very low)"
    Qwen__Qwen3-30B-A3B: "96.0 KiB (Moderate)"
    HuggingFaceTB__SmolLM3-3B-Base: "72.0 KiB (Low)"
    zai-org__GLM-4.5-Air: "184.0 KiB (High)"
    openai__gpt-oss-20b: "48.0 KiB (Low)"
    openai__gpt-oss-120b: "72.0 KiB (Low)"
    deepseek-ai__DeepSeek-V3: "68.6 KiB (Low)"
    deepseek-ai__DeepSeek-V4-Flash: "6.7 KiB (Very low)"
    deepseek-ai__DeepSeek-V4-Pro: "9.6 KiB (Very low)"
    Qwen__Qwen3-Next-80B-A3B-Instruct: "24.0 KiB (Very low)"

# 소프트맥스 attention을 실제로 돌리는 레이어 수. Q 투영(q_proj / query_key_value / c_attn …)
# 보유 여부로 트레이스에서 직접 센다 -- 모듈 이름은 계열마다 제각각이라(Nemotron은 attention도
# Mamba도 전부 `mixer`, Falcon은 `self_attention`) 이름 기반 규칙은 매번 깨졌다.
# 여기 기록해두는 이유: 2026-07-30에 이름 기반 규칙으로 바꿨다가 falcon(32→0)과
# Nemotron(4→0)이 조용히 attention-free로 뒤집혔는데, 두 모델 다 갤러리 대조 목록에 없어서
# 게이트가 그냥 통과시켰다. 이제 이 값이 어긋나면 실패한다.
# active 파라미터 수 — 벤더 발표치와 우리 값이 다를 수 있고, 그게 오류가 아닌 경우.
# 자유 평가(2026-07-30)에서 GLM-4.5-Air가 공식 12B인데 우리는 13.42B로 나와 파고든 결과:
# 공식은 **본체만**(임베딩·lm_head 제외) 센 값이었다. 반대로 DeepSeek-V3는 공식 37B인데
# 임베딩 포함(37.55B)이 더 가깝고 제외하면 35.7B로 멀어진다 — 즉 벤더마다 관례가 다르다.
# 우리는 "토큰 1개 forward가 실제로 거치는 파라미터"로 통일하고 카드에 기준을 명시한다.
# 여기 적어두는 이유: 다음에 같은 불일치를 보고 "버그"로 오판하지 않기 위해서다.
active_param_convention:
  ours: "embedding + lm_head 포함 (토큰 1개가 실제로 거치는 전부)"
  checked_on: "2026-07-30"
  observed_vendor_gaps:
    zai-org__GLM-4.5-Air: "공식 12B = 본체만. 우리 13.42B = 본체 12.18B + embed 0.62B + head 0.62B"
    deepseek-ai__DeepSeek-V3: "공식 37B. 우리 37.55B(임베딩 포함)가 더 근접, 제외 시 35.7B"

attention_layers:
  note: "0 = attention-free(순환/SSM). L과 같으면 전 레이어 attention, 그 사이면 하이브리드."
  checked_on: "2026-07-30"
  values:
    NX-AI__xLSTM-7b: 0
    Qwen__Qwen3-Next-80B-A3B-Instruct: 12
    Zyphra__Zamba2-1.2B: 6
    nvidia__NVIDIA-Nemotron-3-Nano-4B-BF16: 4
    tiiuae__falcon-7b: 32
    deepseek-ai__DeepSeek-V4-Pro: 61
    meta-llama__Llama-3.1-405B: 126
    openai__gpt-oss-20b: 24
    google__gemma-2-2b: 26

# develop/check_value_collisions.py가 찾는 "실제 트레이스 축에서 2개+ 이름이 겹치고, 그 축이
# slice/split/concat/reshape류에 있는" 위험 자리 중 **외부 검토를 거쳐 실제로 해소된 것**.
# develop/verify/collision_baseline.json은 게이트 도입 시점의 전체 스냅샷(아직 검토 전, WARN
# 대상)이고, 여기 등재되면 그 자리는 검토된 것으로 승격돼 더 이상 WARN도 뜨지 않는다. 반대로
# 스냅샷에도 여기에도 없는 자리가 새로 나타나면 develop/verify_all.py가 FAIL로 잡는다 --
# "기존 것 전부 백필" 대신 "새로 생긴 것부터 강제"를 택했다(review/05-overrides.md, 외부
# 검토로 정정, 2026-09-01 -- v1은 3개+ plain 심볼만 봐서 이 항목 자체를 못 잡았었다).
value_collisions_reviewed:
  - model: deepseek-ai__DeepSeek-V4-Pro
    value: 64
    symbols: [c_I-d_rope, d_rope, n_h_I]
    function_reviewed: apply_rotary_pos_emb
    source: >
      modeling_deepseek_v4.py:342-359 apply_rotary_pos_emb -- `nope = x[..., :-rope_dim]`
      (안 도는 부분, 폭 c_I-d_rope) / `rope = x[..., -rope_dim:]`(도는 부분, 폭 d_rope)로
      나뉜다. n_h_I(index_n_heads)는 이 둘과 무관한 head-count 값인데 이 체크포인트에서
      우연히 같은 64다. 트레이스 op_id 1874(slice)/1887(concat), prefill.
    reviewer: codex
    date: "2026-09-01"

  - model: moonshotai__Kimi-K3
    value: 96
    symbols: [n_h, n_h_kda, n_kv]
    function_reviewed: KimiDeltaAttention (qkv projection unflatten)
    source: >
      modeling_kimi_linear.py:495 `projection_size = head_dim * num_heads` (linear_attn_config:
      num_heads=96) -- KDA(layer 0 확인)의 head 개수. MLA의 num_attention_heads/
      num_key_value_heads도 이 체크포인트에서 우연히 96. 위치: op 113/125/130/135/148 등
      self_attn view, prefill/decode 양쪽. 등록된 이름은 이미 n_h_kda로 맞았으나(view의
      OUTPUT 축) 같은 op의 INPUT 쪽 flat 축은 n_h*d_v(MLA 개념)로 잘못 남아 있었다 --
      rules/label_overrides.yaml에 12개 항목(prefill/decode 각 6자리) 추가로 정정
      (2026-09-01).
    reviewer: codex
    date: "2026-09-01"

  - model: moonshotai__Kimi-K3
    value: 128
    symbols: [d_head_kda, d_nope, d_v]
    function_reviewed: KimiDeltaAttention (qkv projection unflatten)
    source: >
      modeling_kimi_linear.py:495,541,658 -- KDA의 head_dim=128(linear_attn_config).
      MLA의 qk_nope_head_dim/v_head_dim도 이 체크포인트에서 우연히 128. 위 96 항목과 같은
      자리, 같은 정정(n_h*d_v -> n_h_kda*d_head_kda)으로 함께 해소됨.
    reviewer: codex
    date: "2026-09-01"

  - model: moonshotai__Kimi-K3
    value: 64
    symbols: [d_chunk, d_rope]
    function_reviewed: naive_chunk_kda
    source: >
      fla/ops/kda/naive.py:63-108 (naive_chunk_kda, FLA 커밋 35dceaee54) -- BT=chunk_size
      기본값 64. op 162(view)/164(permute), self_attn, layer 0(KDA). 이미 d_chunk로
      올바르게 렌더되고 있었다(rules/label_overrides.yaml의 인접 "5"->n_chunk 교정,
      2026-08-27, 그 앵커 shape 자체가 d_chunk를 literal로 인용). d_rope는 값만 같을 뿐
      실제로 쓰이지 않는 후보 -- 코드 변경 불필요, 검토만 등재.
    reviewer: codex
    date: "2026-09-01"

  - model: moonshotai__Kimi-K3
    value: 6144
    symbols: [2*d_moe, E_shared*d_moe]
    function_reviewed: KimiSparseMoeBlock shared-expert MLP
    source: >
      modeling_kimi_linear.py:698-734 -- intermediate_size = moe_intermediate_size *
      num_shared_experts (E_shared=2, d_moe=3072). concat 두 입력과 뒤이은 slice 출력이
      전부 E_shared*d_moe로 이미 올바르게 렌더되고 있었다. 2*d_moe는 E_shared=2와
      우연히 같은 값일 뿐인 미사용 후보 -- 코드 변경 불필요, 검토만 등재. (concat의
      OUTPUT 축(12288)은 이 충돌의 대상이 아니며 별건 -- 4*d_moe로 렌더되는데 이것도
      산술적으로는 맞지만(4*d_moe == 2*E_shared*d_moe) 더 명료한 이름으로 바꿀 여지가
      있다는 게 외부 검토의 제안, 값 충돌은 아니라 이번엔 반영하지 않음.)
    reviewer: codex
    date: "2026-09-01"

# config에 필드가 없어 modeling 코드에서만 확인되는 사실. resolve_symbols가 결정적으로 채운다.
# 여기 적어두는 이유: alias로 안 잡히는 값을 코드에 하드코딩하면 근거가 사라지기 때문이다.
modeling_sourced_symbols:
  checked_on: "2026-07-30"
  n_sink:
    value: 1
    models: [openai__gpt-oss-20b, openai__gpt-oss-120b]
    gate: "config.model_type == 'gpt_oss'"
    source: >
      transformers 5.14.1 models/gpt_oss/modeling_gpt_oss.py — GptOssAttention.sinks는 head당
      스칼라 1개이고, score 행렬에 **1열**로 concat된다(head당 1열이 아니라 축 하나에 +1).
      교차검증: gpt-oss 모델 카드(arXiv:2508.10925) "the denominator of the softmax has an
      additional additive value". rules/structures/attention/attention-sink.md 참고.
    note: >
      deepseek_v4도 같은 기법을 쓰지만 여기 넣지 않는다 — V4의 score 폭은 derived_dims.yaml에
      `T + T/m_csa + 1` 형태로 이미 명시돼 있고, n_sink를 켜면 일반 규칙 `T + 1 + n_sink`가
      score 폭이 아닌 V4 축에 발동할 수 있다.

# 아키텍처 차원이 아니라 op 자체의 구조 상수라, 심볼을 붙이면 오히려 거짓이 되는 값들.
# verify_all.py가 "설명 안 된 bare 정수"를 셀 때 이건 정상으로 간주한다.
# 2026-07-31 자유 평가 이후 갱신. 휴리스틱이 스코프 밖 심볼로 식을 지어내던 것을 막자,
# 그동안 가짜 이름에 가려져 있던 진짜 기약 정수들이 드러났다. 아래는 전수 확인한 목록이며
# 이 밖에 남은 정수는 없다(V4-Pro {2,3,8}, V4-Flash {2,3}, Qwen3-Next는 언롤 경계뿐).
# 기계 검사용 `values:` 를 함께 둔다. 이 목록에 없는 정수가 산출물에 나오면 게이트가
# WARN 으로 띄운다 — "이름이 없다"와 "이름이 없는 이유를 안다"를 구분하기 위해서다.
# 2026-08-06 기준 함대의 모든 bare 정수가 여기 덮인다.
#
# `values: any_small_odd_or_scan`(freeform, 특정 상한 없이 "루프 언롤 계단 전체")는
# `max_value` 를 반드시 같이 적는다 -- 2026-08-27 발견: 이 필드가 없으면
# `develop/verify_all.py:check_documented_literals` 가 그 **모델 전체**를 검사에서
# 건너뛴다(Codex 검토, Kimi-K3 재트레이스 검증 중 발견). `max_value` 는 그 모델을
# 통째로 봐주는 대신 "이 값 이하만" 봐주게 하는 안전장치다 -- 그 값을 넘는 새 미확인
# 정수는 여전히 잡혀야 한다.
irreducible_literals:
  - value_note: "복소수 pair 분할(view_as_real / stack→view)의 마지막 축 2"
    values: [2]
    models: [all]
  - value_note: >
      Gated DeltaNet 청크 스캔의 언롤된 루프 경계. `linear_attn` 안의 slice/clone 이
      반복마다 한 칸씩 넓은 조각을 떼며 3,4,5,…의 연속 사다리를 만든다(모델마다 같은
      횟수씩 — Qwen3.6-27B 는 각 값이 정확히 288 회). 반복 계단이지 아키텍처 상수가 아니다.
      경계는 `fla/ops/common/chunk_delta_h.py`의 `chunk_size=64` -- Kimi-K3의 KDA와 같은
      `fla` 공용 청킹 관례라 사다리 상한도 63으로 같다(2026-08-27 정정 -- 원래 "…,10"까지만
      적었던 건 예시를 잘라 쓴 것이지 실제 상한이 아니었다. freeform 게이트 구멍을 고치며
      값 11~18이 미문서 상태로 새로 드러나서 발견함).
      예전에는 홀수만 정수로 남고 짝수는 값이 겹치는 심볼 이름을 달고 있었는데(n_kv=2,
      d_conv_lin=4, k=10 …), 그 지어낸 이름을 떼면서 사다리 전체가 드러났다
      (build_table._unname_refilled_operands, 2026-08-12).
    values: any_small_odd_or_scan
    max_value: 63
    models: [Qwen__Qwen3-Next-80B-A3B-Instruct, Qwen__Qwen3.5-4B, Qwen__Qwen3.6-27B,
             Qwen__Qwen3.6-35B-A3B, Qwen__Qwen3.5-397B-A17B]
  - value_note: >
      위 사다리(경계 63)와 같은 청크 스캔의 청크 크기 자체 64 -- 반복 카운터가 아니라
      `torch_chunk_gated_delta_rule`(transformers/models/qwen3_next/modeling_qwen3_next.py:380)의
      `chunk_size=64` 기본값이다. `torch.triu(torch.ones(chunk_size, chunk_size), ...)` 마스크와
      `g.cumsum(dim=-1).exp()`로 만든 decay 텐서가 이 값을 그대로 축 크기로 남긴다. 사다리는
      1..63(반복 카운터, 위 freeform 항목)까지만 묶이므로 청크 크기 자체인 64는 그 경계
      밖이라 별도로 등재해야 한다(2026-08-29, 세 번의 `_unname_loop_indices` 수정 시도가
      전부 회귀해 되돌린 뒤 이 경로로 정리 -- 코드가 아니라 트레이스에 있는 실제 상수임을
      확인했으므로 여기 등재하는 것으로 끝난다).
    values: [64]
    models: [Qwen__Qwen3-Next-80B-A3B-Instruct, Qwen__Qwen3.5-4B, Qwen__Qwen3.6-27B,
             Qwen__Qwen3.6-35B-A3B, Qwen__Qwen3.5-397B-A17B]
  - value_note: >
      KDA(Kimi Delta Attention) 청크 스캔의 인트라-청크 순차 재귀 루프 경계 -- 위 Gated
      DeltaNet 항목과 같은 부류. `fla/ops/kda/naive.py:134` `for i in range(1, BT)`
      (BT=chunk_size=64)가 청크 내부를 한 칸씩 넓혀가며 순차 처리하고, 그 각 반복의
      슬라이스 크기가 `self_attn` 모듈 안에 3,4,5,…의 사다리로 그대로 남는다(Kimi-K3
      실측: 값마다 레이어당 정확히 같은 횟수, 예 5/6/7이 각 1275회). 반복 계단이지
      아키텍처 상수가 아니다.
    values: any_small_odd_or_scan
    max_value: 63
    models: [moonshotai__Kimi-K3]
  - value_note: >
      Kimi-K3 MoE 캡 셔플의 부산물 1280(prefill) -- `src/kda_shim.py`의 `patch_moe_infer`가
      `KDA_SHIM_EXPERT_CAP=4`(기본값)로 토큰을 4명의 전문가에게 균등 분할하며 생기는 값이다:
      `1280 = k(16)·T(320)/4`. 아키텍처 차원이 아니라 우리가 넣은 근사(shim)의 산술 부산물이라
      이름을 붙이지 않는다 -- 이름을 붙이면 shim을 아키텍처로 오해하게 만든다.
      review/06-open-renames.md A60 참고. `provenance.adaptation_log`의
      `moe_infer_even_split`으로도 기록되고 C10 예외(`_expert_cap_gap`, src/run.py)로도
      처리된 것과 같은 부류다.
    values: [1280]
    models: [moonshotai__Kimi-K3]
  - value_note: >
      mHC의 scale 파라미터 개수 3 — pre/post/comb 세 게이트 계열에 각각 하나씩이며
      `unbind [3] -> [], [], []`로 나타난다. config 값이 아니라 모듈 구조 상수다.
      rules/structures/residual/mhc.md의 `attn_hc.scale [3]` 참고.
      (0731 리비전은 `attn_hc.scale`/`ffn_hc.scale` 로 같은 구조를 그대로 갖는다.)
    values: [3]
    models: [deepseek-ai__DeepSeek-V4-Pro, deepseek-ai__DeepSeek-V4-Flash,
             deepseek-ai__DeepSeek-V4-Flash-0731]
  - value_note: >
      LFM2 short-conv 블록의 18·32 — `model.layers.*.conv` 안에서만 나타나고 config 어느
      필드와도 대응되지 않는다. 커널 폭 3은 `conv_L_cache` 로 접지했으나(d_conv), 나머지 둘은
      소스에서 근거를 찾지 못했다. 이름을 지어내지 않고 정수로 둔다(Tier 3).
    values: [18, 32]
    models: [LiquidAI__LFM2-8B-A1B]
  - value_note: >
      Hunyuan-A13B `mlp.shared_mlp` 의 3072. 이 모델은 intermediate_size 와
      moe_intermediate_size 가 둘 다 3072 인 순수 MoE 라, 겹치는 이름 하나를 버리는 규칙에
      따라 `d_ff` 가 후보에서 빠지고 `d_moe` 는 스코프(`expert|moe`)가 `shared_mlp` 를 안 덮는다.
      d_moe 스코프에 `shared` 를 넣어 봤으나 되돌렸다 — 스코프가 문자열 더 앞에서 매치되면서
      "안쪽 스코프가 이긴다" 규칙이 뒤집혀 Qwen3-Next 의 shared_expert 폭이 `d_moe` 에서
      `E`(전문가 수, 값이 같음)로 퇴행했다. 값은 맞지만 이름을 못 붙이는 자리로 남긴다(Tier 3).
    values: [3072]
    models: [tencent__Hunyuan-A13B-Instruct]
  - value_note: >
      V4-Pro Lightning Indexer의 8 — `new_zeros [B,d_head,n_hc,d_head/2]`로 잡은 버퍼를
      `[B,d_head,8,c_I]`로 재해석했다가(4·256 = 8·128) 곧바로 n_hc로 slice해 되돌린다.
      값은 2·n_hc지만 아키텍처 차원이라는 근거를 못 찾았으므로 이름을 붙이지 않는다(Tier 3).
    values: [8]
    models: [deepseek-ai__DeepSeek-V4-Pro]

context_length_public:
  # config 의 `max_position_embeddings`(= `ctx`)와 **공급자가 공개한 컨텍스트 길이**가 다를 수
  # 있다. 둘 다 사실이고 섞으면 안 된다 -- config 값은 이 checkpoint 가 들고 있는 값이고,
  # 공개값은 모델 카드가 말하는 지원 길이다. 외부 검토(2026-09-11)가 Llama-4 에서 짚었다.
  source: "각 모델의 공식 모델 카드"
  checked_on: "2026-09-11"
  note: >
    우리 `ctx` 는 **config 값**이다. 여기 값과 다르면 그 자체가 기록할 사실이지,
    맞추려고 `ctx` 를 고치지 않는다(01-main.md P1).
  values:
    meta-llama__Llama-4-Maverick-17B-128E:
      config: 262144
      public: 1048576
      source: "https://github.com/meta-llama/llama-models/blob/main/models/llama4/MODEL_CARD.md"
      note: "Meta 공식 모델 카드는 Maverick 을 128 experts / 1M context 로 명시한다."
