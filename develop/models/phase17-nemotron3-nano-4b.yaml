# PASSED 01-main.md §9 checklist (C10,C16 focus + C13 repro), Phase 17.
# nemotron_h hybrid: 42 layers = Mamba-2 (linear_attention) 21 + full_attention 4 (GQA 40:8) +
# mlp 17. Mamba SSM traced via naive fallback; all 263 params covered (C10). Dense FFN
# (n_routed_experts=8 is vestigial -> C8 WARN). Needed loader.py 'mlp' cache-dispatch shim.
# canary tags: hybrid-attention, Mamba, SSM, dense.
model_id: nvidia/NVIDIA-Nemotron-3-Nano-4B-BF16
revision: null
phases: [prefill, decode]
seq_len: auto
overrides:
  attn_implementation: auto
extra_entrypoints: auto
