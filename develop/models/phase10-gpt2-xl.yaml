# PASSED 01-main.md §9 checklist (C1,C6 required + C13 repro), Phase 10.
# Pure MHA baseline (n_h=25, no GQA/MQA), learned absolute pos (wpe, not RoPE), 48 layers.
# Drove scope.py generalization: decoder stack is transformer.h.N ('h', not 'layers').
# canary tags: MHA-baseline, learned-abs-pos, gpt2-h-naming.
model_id: openai-community/gpt2-xl
revision: null
phases: [prefill, decode]
seq_len: auto
overrides:
  attn_implementation: auto
extra_entrypoints: auto
