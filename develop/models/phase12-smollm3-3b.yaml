# PASSED 01-main.md §9 checklist (C2 required + C13 repro), Phase 12.
# NoPE: RoPE every layer EXCEPT every 4th (no_rope_layers=[1,1,1,0,...]). GQA 16:4.
# Drove C2 generalization: expected clusters from ALL per-layer schedule lists (layer_types +
# no_rope_layers), not layer_types alone. canary tags: NoPE, GQA, heterogeneous-schedule.
model_id: HuggingFaceTB/SmolLM3-3B-Base
revision: null
phases: [prefill, decode]
seq_len: auto
overrides:
  attn_implementation: auto
extra_entrypoints: auto
