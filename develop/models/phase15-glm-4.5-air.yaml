# PASSED 01-main.md §9 checklist (C2,C8 required + C13 repro), Phase 15.
# dense-prefix (first_k_dense_replace=1) + shared(1) + routed(128 top-8) MoE, GQA 96:8. Same
# family as DeepSeek, different company -> generalizes dense-prefix+shared. MTP native-omitted
# (C15 WARN). canary tags: MoE-shared-expert, dense-prefix-schedule, GQA.
model_id: zai-org/GLM-4.5-Air
revision: null
phases: [prefill, decode]
seq_len: auto
overrides:
  attn_implementation: auto
extra_entrypoints: auto
