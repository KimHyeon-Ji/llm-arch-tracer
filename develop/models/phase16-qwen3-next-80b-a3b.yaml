# PASSED 01-main.md §9 checklist (C2,C16 focus + C13 repro), Phase 16.
# Gated DeltaNet (linear attention) + gated full-attention hybrid, 3:1 (36 DeltaNet + 12 full),
# GQA 16:2, MoE E=512 top-10 + shared. DeltaNet traced via torch fallback (no fast-path kernel).
# canary tags: hybrid-attention, DeltaNet, linear-attention, gated-attention, MoE-shared-expert.
model_id: Qwen/Qwen3-Next-80B-A3B-Instruct
revision: null
phases: [prefill, decode]
seq_len: auto
overrides:
  attn_implementation: auto
extra_entrypoints: auto
