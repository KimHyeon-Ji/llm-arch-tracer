# PASSED 01-main.md §9 checklist, Phase 8. No FAIL; C15 WARN documents that config declares
# MTP but the native transformers deepseek_v3 impl omits the MTP head (user decision: accept
# native limitation, surface via C15 WARN). Main forward fully traced. C13 reproducible.
# canary tags: MTP-entrypoint (native-omitted), deepseek-v3-arch.
model_id: bzantium/tiny-deepseek-v3
revision: null
phases: [prefill, decode]
seq_len: auto
overrides:
  attn_implementation: auto
extra_entrypoints: auto
