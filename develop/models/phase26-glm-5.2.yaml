# GLM-5.2 (Zhipu/Z.ai). model_type=glm_moe_dsa — DeepSeek Sparse Attention(DSA) + MoE.
# config 에 index_head_dim / index_n_heads / index_topk 가 있어 DeepSeek-V4 의 indexer 와
# 같은 계열로 보이고, 우리 n_h_I / c_I / k_I 심볼이 전이되는지 확인하는 케이스.
# L=78, d=6144, n_h=64, head_dim=192, E=256, first_k_dense_replace=3.
model_id: zai-org/GLM-5.2
revision: null
phases: [prefill, decode]
seq_len: auto
overrides:
  attn_implementation: auto
extra_entrypoints: auto
