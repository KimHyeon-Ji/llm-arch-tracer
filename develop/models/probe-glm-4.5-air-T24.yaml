# 축 분리 probe — promote 하지 않는다. --out develop/probe_out 으로만 돌린다.
# GLM-4.5-Air 는 T=16, k=8 이라 k*T = 128 = E 로 전문가 수와 선택쌍 수가 겹친다.
# T=24 면 k*T = 192 != 128 이라 두 축이 갈린다.
model_id: zai-org/GLM-4.5-Air
revision: null
phases: [prefill, decode]
seq_len: 24
overrides:
  attn_implementation: auto
extra_entrypoints: auto
