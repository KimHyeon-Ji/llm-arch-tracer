# PASSED 01-main.md §9 checklist (C5,C6,C8 required + C13 repro), Phase 6.
# MLA + shared+routed MoE + dense-prefix combo. Loaded via NATIVE transformers deepseek_v2
# (repo remote code is pinned to old transformers, breaks on 5.x -- provenance.needs_remote_code).
# canary tags: MLA, MoE-shared+routed, combo.
model_id: deepseek-ai/DeepSeek-V2-Lite
revision: null
phases: [prefill, decode]
seq_len: auto
overrides:
  attn_implementation: auto
extra_entrypoints: auto
