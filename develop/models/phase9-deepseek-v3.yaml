# PASSED 01-main.md §9 checklist, Phase 9 (scale-complex). DeepSeek-V3 671B/37B: MLA +
# DeepSeekMoE (E=256 top-8 + shared, dense-prefix 3) + sigmoid gating + aux-loss-free bias.
# 61 layers traced on meta, no FAIL, C13 repro. C15 WARN (native omits MTP, accepted). Native
# transformers deepseek_v3. canary tags: scale-complex, MLA, MoE-shared+routed, sigmoid-gating.
model_id: deepseek-ai/DeepSeek-V3
revision: null
phases: [prefill, decode]
seq_len: auto
overrides:
  attn_implementation: auto
extra_entrypoints: auto
