# PASSED 01-main.md §9 checklist (C5,C6 required + C13 repro), Phase 11.
# MHA + QK-Norm (q_norm/k_norm) + post-norm placement (post_attention/feedforward_layernorm,
# no input_layernorm). All RMSNorm. C5 PASS despite post-norm (residual d_model preserved).
# canary tags: MHA, QK-Norm, post-norm-variant.
model_id: allenai/OLMo-2-1124-7B-Instruct
revision: null
phases: [prefill, decode]
seq_len: auto
overrides:
  attn_implementation: auto
extra_entrypoints: auto
