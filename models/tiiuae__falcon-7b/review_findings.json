{
  "model_id": "tiiuae/falcon-7b",
  "reviewed_on": "2026-08-11",
  "reviewer": "llm(claude, 전수 점검 2회차 — 모듈-필드 소속)",
  "angle": "전수 점검 2회차 — 1층(모듈-필드 소속: 가중치 축의 이름이 그 모듈/부모가 실제로 읽는 config 필드에서 나왔는가)을 함대 전건 수행. 값을 보지 않는 검사라 값 충돌이 숨길 수 없다. 1회차의 A절·B절 판정은 유지. C절(모듈별 출력 shape)은 여전히 미수행.",
  "summary": "의뢰서 3건 중 1건이 실제 오라벨(FFN 폭), 2건은 오탐이었다.",
  "findings": [
    {
      "module": "(config)",
      "axis": "d_head 가 읽은 head_dim",
      "current_label": "head_dim",
      "verdict": "current_label_correct",
      "proposed_label": "",
      "confidence": "high",
      "evidence": "`configuration_falcon.py:102` `@property def head_dim(self): return self.hidden_size // self.num_attention_heads` — 클래스가 프로퍼티로 정의한다. 탐지기가 프로퍼티를 못 읽던 것이 원인이라 `src/source_check.py._config_fields` 에 프로퍼티 수집을 넣었다.",
      "status": "fixed"
    },
    {
      "module": "transformer.h.*.mlp.dense_h_to_4h / dense_4h_to_h",
      "axis": "FFN 중간 폭 18176",
      "current_label": "4*d_model",
      "verdict": "should_be_renamed",
      "proposed_label": "d_ff",
      "confidence": "high",
      "evidence": "`modeling_falcon.py:531` `self.dense_h_to_4h = FalconLinear(hidden_size, config.ffn_hidden_size, ...)` + `configuration_falcon.py:96` `if self.ffn_hidden_size is None: self.ffn_hidden_size = self.hidden_size * 4`. 폭의 이름은 `ffn_hidden_size` 이고 4배는 그 기본값일 뿐이다. `rules/symbols.yaml` 의 d_ff 별칭에 `ffn_hidden_size` 를 추가했다.",
      "status": "fixed"
    }
  ]
}