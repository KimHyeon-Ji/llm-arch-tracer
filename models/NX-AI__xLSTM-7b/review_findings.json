{
  "model_id": "NX-AI/xLSTM-7b",
  "reviewed_on": "2026-08-12",
  "reviewer": "llm(claude, 외부 검토 지적 반영 + 미답변 4건 판정)",
  "angle": "외부 검토가 지적한 3건(layer_sched 소실 / 검토 미수행 / 가중치-피연산자 불일치)을 재현·반영하고, 새로 배선한 미답변 검사가 잡은 4건에 답했다.",
  "summary": "의뢰서 3건 전부 오탐이었다. 라벨이 옳다.",
  "findings": [
    {
      "module": "(config)",
      "axis": "d_v 가 읽은 v_head_dim",
      "current_label": "v_head_dim",
      "verdict": "current_label_correct",
      "proposed_label": "",
      "confidence": "high",
      "evidence": "`configuration_xlstm.py:179` `@property def v_head_dim(self): return self.v_dim // self.num_heads`. v_dim_factor=1.0 이라 v_dim=embedding_dim=4096, 4096/8=512 로 실측과 일치.",
      "status": "fixed"
    },
    {
      "module": "(config)",
      "axis": "d_head 가 읽은 head_dim",
      "current_label": "head_dim",
      "verdict": "current_label_correct",
      "proposed_label": "",
      "confidence": "high",
      "evidence": "체크포인트 config.json 이 `head_dim: 512` 를 직접 준다(HF 원본 확인). qk_dim_factor=0.5 라 Q/K 의 head 폭은 256 이고 **그 축들은 d_head 가 아니라 `d_model*qk_f` 로 올바르게 렌더되고 있다**(실측: q/k 가중치 `[d_model*qk_f, d_model]`).",
      "status": "fixed"
    },
    {
      "module": "model.blocks.*.mlstm_layer.v / out_proj",
      "axis": "[d_model, d_model]",
      "current_label": "d_model (두 번)",
      "verdict": "current_label_correct",
      "proposed_label": "",
      "confidence": "high",
      "evidence": "v_dim_factor=1.0 이라 V 투영이 정사각 `[4096, 4096]` 이고 out_proj 도 마찬가지다. 정사각 reshape 이 아니라 **정사각 가중치**이므로 같은 이름 두 번이 정상 — 탐지기가 reshape 만 찾아서 미확인으로 남겼다.",
      "status": "fixed"
    },
    {
      "module": "backbone.blocks.*.mlstm_layer.mlstm_backend",
      "axis": "head 폭 (18,752축)",
      "current_label": "d_head (스코프 밖)",
      "verdict": "should_be_renamed",
      "proposed_label": "d_head (스코프 확장)",
      "confidence": "high",
      "status": "fixed",
      "evidence": "`n_h` 는 스코프에 `mlstm|mixer` 를 갖는데 `d_head` 는 `attn|attention|rotary` 뿐이었다. head 수가 정당한 자리에서는 head 폭도 정당하므로 비대칭이다 — mLSTM 백엔드 안의 head 폭 18,752축이 스코프 밖 이름으로 붙어 있었고, 폴백이 막히면 정수로 떨어질 자리였다. `d_head` 스코프에 `mlstm|mixer` 를 추가했다(게이트: 퇴행 0 / 개선 1). A절 함대 스윕에서 드러났다."
    },
    {
      "module": "backbone",
      "axis": "mLSTM 상태 버퍼의 head 축·head 폭",
      "current_label": "n_h / d_head (스코프 밖 폴백)",
      "verdict": "current_label_correct",
      "proposed_label": "",
      "confidence": "high",
      "status": "open",
      "evidence": "xLSTM 은 mLSTM 순환 상태 버퍼를 블록 안이 아니라 **스택 루트**에서 할당한다 — `zeros() -> [B, n_h, d_model*qk_f/n_h, d_head]` (실측 `[1,8,256,512]`). 두 이름 다 정확히 맞고, 근거만 '스코프 밖 폴백'이라 약하다. 스코프에 `backbone` 을 추가해 봤으나 **퇴행했다** — 휴리스틱 0 → 768, flow_ambig 0 → 192. 루트는 온갖 텐서가 지나가는 자리라 거기서 head 이름을 in-scope 로 올리면 다른 축까지 가져간다. 되돌리고 남긴다."
    }
  ]
}