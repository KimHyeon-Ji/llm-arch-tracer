[
 {
  "id": "[\"self_attn$\", \"n_h\", \"n_kv\", 4, \"class\", 1, null, [\"B\", \"n_h\", \"T\", \"d_head\"], \"o\", 0, \"transpose\", 1, null]",
  "from": "n_h",
  "to": "n_kv",
  "module": "self_attn$",
  "expect": 4,
  "source": "transformers 5.14.1 modeling_llama.py `LlamaAttention.forward` 는 q/k/v 를 순서대로 `view(hidden_shape).transpose(1, 2)` 한다. 따라서 이 모듈의 transpose 서수는 nth0=query, nth1=key, nth2=value 다. key/value 의 head 축은 `num_key_value_heads` 이므로 n_kv 다. 이 모델은 n_h == n_kv == 4 라 값으로는 못 가리고, 트레이스에서 부모를 거슬러 보면 nth1 은 k_proj, nth2 는 v_proj 를 쓴다(같은 자리를 Llama-3.1-405B 는 n_kv 로 이미 올바르게 렌더한다 -- 거기서는 n_h=128 != n_kv=8 이라 값으로 갈린다). (이 항목은 key.)\n",
  "applied": 32,
  "vetoed": 0
 },
 {
  "id": "[\"self_attn$\", \"n_h\", \"n_kv\", 4, \"class\", 1, null, [\"B\", \"n_h\", \"1\", \"d_head\"], \"o\", 0, \"transpose\", 1, null]",
  "from": "n_h",
  "to": "n_kv",
  "module": "self_attn$",
  "expect": 4,
  "source": "transformers 5.14.1 modeling_llama.py `LlamaAttention.forward` 는 q/k/v 를 순서대로 `view(hidden_shape).transpose(1, 2)` 한다. 따라서 이 모듈의 transpose 서수는 nth0=query, nth1=key, nth2=value 다. key/value 의 head 축은 `num_key_value_heads` 이므로 n_kv 다. 이 모델은 n_h == n_kv == 4 라 값으로는 못 가리고, 트레이스에서 부모를 거슬러 보면 nth1 은 k_proj, nth2 는 v_proj 를 쓴다(같은 자리를 Llama-3.1-405B 는 n_kv 로 이미 올바르게 렌더한다 -- 거기서는 n_h=128 != n_kv=8 이라 값으로 갈린다). (이 항목은 key.)\n",
  "applied": 34,
  "vetoed": 0
 },
 {
  "id": "[\"self_attn$\", \"n_h\", \"n_kv\", 4, \"class\", 1, null, [\"B\", \"n_h\", \"T\", \"d_head\"], \"o\", 0, \"transpose\", 2, null]",
  "from": "n_h",
  "to": "n_kv",
  "module": "self_attn$",
  "expect": 4,
  "source": "transformers 5.14.1 modeling_llama.py `LlamaAttention.forward` 는 q/k/v 를 순서대로 `view(hidden_shape).transpose(1, 2)` 한다. 따라서 이 모듈의 transpose 서수는 nth0=query, nth1=key, nth2=value 다. key/value 의 head 축은 `num_key_value_heads` 이므로 n_kv 다. 이 모델은 n_h == n_kv == 4 라 값으로는 못 가리고, 트레이스에서 부모를 거슬러 보면 nth1 은 k_proj, nth2 는 v_proj 를 쓴다(같은 자리를 Llama-3.1-405B 는 n_kv 로 이미 올바르게 렌더한다 -- 거기서는 n_h=128 != n_kv=8 이라 값으로 갈린다). (이 항목은 value.)\n",
  "applied": 16,
  "vetoed": 0
 },
 {
  "id": "[\"self_attn$\", \"n_h\", \"n_kv\", 4, \"class\", 1, null, [\"B\", \"n_h\", \"1\", \"d_head\"], \"o\", 0, \"transpose\", 2, null]",
  "from": "n_h",
  "to": "n_kv",
  "module": "self_attn$",
  "expect": 4,
  "source": "transformers 5.14.1 modeling_llama.py `LlamaAttention.forward` 는 q/k/v 를 순서대로 `view(hidden_shape).transpose(1, 2)` 한다. 따라서 이 모듈의 transpose 서수는 nth0=query, nth1=key, nth2=value 다. key/value 의 head 축은 `num_key_value_heads` 이므로 n_kv 다. 이 모델은 n_h == n_kv == 4 라 값으로는 못 가리고, 트레이스에서 부모를 거슬러 보면 nth1 은 k_proj, nth2 는 v_proj 를 쓴다(같은 자리를 Llama-3.1-405B 는 n_kv 로 이미 올바르게 렌더한다 -- 거기서는 n_h=128 != n_kv=8 이라 값으로 갈린다). (이 항목은 value.)\n",
  "applied": 18,
  "vetoed": 0
 },
 {
  "id": "[\"self_attn$\", \"n_h\", \"n_kv\", 4, \"class\", 2, null, [\"B\", \"T\", \"n_h\", \"d_head\"], \"o\", 0, \"view\", 1, null]",
  "from": "n_h",
  "to": "n_kv",
  "module": "self_attn$",
  "expect": 4,
  "source": "transformers 5.14.1 modeling_llama.py:262 는 k/v 를 `view(hidden_shape)` 로 펴는데 그 head 축은 `num_key_value_heads` 다. 이 모델은 n_h == n_kv == 4 라 값으로는 못 가리고, 트레이스에서 부모를 거슬러 보면 이 자리는 k_proj.weight 를 쓴다. **repeat_kv 이후가 아니라 이전**이므로 n_kv 가 맞다 -- 이 모델은 n_rep == 1 이라 `if n_rep == 1: return` 으로 경계 op 이 트레이스에 없으니, 이 계보를 끝까지 n_kv 로 밀면 안 된다(외부 검토 지적).\n",
  "applied": 4,
  "vetoed": 0
 },
 {
  "id": "[\"self_attn$\", \"n_h\", \"n_kv\", 4, \"class\", 2, null, [\"B\", \"1\", \"n_h\", \"d_head\"], \"o\", 0, \"view\", 1, null]",
  "from": "n_h",
  "to": "n_kv",
  "module": "self_attn$",
  "expect": 4,
  "source": "transformers 5.14.1 modeling_llama.py:262 는 k/v 를 `view(hidden_shape)` 로 펴는데 그 head 축은 `num_key_value_heads` 다. 이 모델은 n_h == n_kv == 4 라 값으로는 못 가리고, 트레이스에서 부모를 거슬러 보면 이 자리는 k_proj.weight 를 쓴다. **repeat_kv 이후가 아니라 이전**이므로 n_kv 가 맞다 -- 이 모델은 n_rep == 1 이라 `if n_rep == 1: return` 으로 경계 op 이 트레이스에 없으니, 이 계보를 끝까지 n_kv 로 밀면 안 된다(외부 검토 지적).\n",
  "applied": 4,
  "vetoed": 0
 },
 {
  "id": "[\"self_attn$\", \"n_h\", \"n_kv\", 4, \"class\", 2, null, [\"B\", \"T\", \"n_h\", \"d_head\"], \"o\", 0, \"view\", 2, null]",
  "from": "n_h",
  "to": "n_kv",
  "module": "self_attn$",
  "expect": 4,
  "source": "transformers 5.14.1 modeling_llama.py:262 는 k/v 를 `view(hidden_shape)` 로 펴는데 그 head 축은 `num_key_value_heads` 다. 이 모델은 n_h == n_kv == 4 라 값으로는 못 가리고, 트레이스에서 부모를 거슬러 보면 이 자리는 v_proj.weight 를 쓴다. **repeat_kv 이후가 아니라 이전**이므로 n_kv 가 맞다 -- 이 모델은 n_rep == 1 이라 `if n_rep == 1: return` 으로 경계 op 이 트레이스에 없으니, 이 계보를 끝까지 n_kv 로 밀면 안 된다(외부 검토 지적).\n",
  "applied": 4,
  "vetoed": 0
 },
 {
  "id": "[\"self_attn$\", \"n_h\", \"n_kv\", 4, \"class\", 2, null, [\"B\", \"1\", \"n_h\", \"d_head\"], \"o\", 0, \"view\", 2, null]",
  "from": "n_h",
  "to": "n_kv",
  "module": "self_attn$",
  "expect": 4,
  "source": "transformers 5.14.1 modeling_llama.py:262 는 k/v 를 `view(hidden_shape)` 로 펴는데 그 head 축은 `num_key_value_heads` 다. 이 모델은 n_h == n_kv == 4 라 값으로는 못 가리고, 트레이스에서 부모를 거슬러 보면 이 자리는 v_proj.weight 를 쓴다. **repeat_kv 이후가 아니라 이전**이므로 n_kv 가 맞다 -- 이 모델은 n_rep == 1 이라 `if n_rep == 1: return` 으로 경계 op 이 트레이스에 없으니, 이 계보를 끝까지 n_kv 로 밀면 안 된다(외부 검토 지적).\n",
  "applied": 4,
  "vetoed": 0
 },
 {
  "id": "[\"self_attn$\", \"n_h\", \"n_kv\", 4, \"class\", 1, null, [\"B\", \"n_h\", \"T\", \"d_head/2\"], \"o\", 0, \"slice\", 3, null]",
  "from": "n_h",
  "to": "n_kv",
  "module": "self_attn$",
  "expect": 4,
  "source": "transformers 5.14.1 modeling_llama.py:262 는 k/v 를 `view(hidden_shape)` 로 펴는데 그 head 축은 `num_key_value_heads` 다. 이 모델은 n_h == n_kv == 4 라 값으로는 못 가리고, 트레이스에서 부모를 거슬러 보면 이 자리는 k_proj.weight (rotate_half(k) 계보 -- modeling_llama.py:138 의 apply_rotary_pos_emb) 를 쓴다. **repeat_kv 이후가 아니라 이전**이므로 n_kv 가 맞다 -- 이 모델은 n_rep == 1 이라 `if n_rep == 1: return` 으로 경계 op 이 트레이스에 없으니, 이 계보를 끝까지 n_kv 로 밀면 안 된다(외부 검토 지적).\n",
  "applied": 6,
  "vetoed": 0
 },
 {
  "id": "[\"self_attn$\", \"n_h\", \"n_kv\", 4, \"class\", 1, null, [\"B\", \"n_h\", \"1\", \"d_head/2\"], \"o\", 0, \"slice\", 3, null]",
  "from": "n_h",
  "to": "n_kv",
  "module": "self_attn$",
  "expect": 4,
  "source": "transformers 5.14.1 modeling_llama.py:262 는 k/v 를 `view(hidden_shape)` 로 펴는데 그 head 축은 `num_key_value_heads` 다. 이 모델은 n_h == n_kv == 4 라 값으로는 못 가리고, 트레이스에서 부모를 거슬러 보면 이 자리는 k_proj.weight (rotate_half(k) 계보 -- modeling_llama.py:138 의 apply_rotary_pos_emb) 를 쓴다. **repeat_kv 이후가 아니라 이전**이므로 n_kv 가 맞다 -- 이 모델은 n_rep == 1 이라 `if n_rep == 1: return` 으로 경계 op 이 트레이스에 없으니, 이 계보를 끝까지 n_kv 로 밀면 안 된다(외부 검토 지적).\n",
  "applied": 6,
  "vetoed": 0
 }
]