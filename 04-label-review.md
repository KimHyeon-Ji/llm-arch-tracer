# 04. 라벨 검토 — 파이프라인에서의 위치

규칙 게이트(`develop/verify_all.py`)는 산출물이 **자기 규칙과 일관되는지**만 판정한다.
규칙 자체가 틀렸거나, 두 config 값이 이 seq_len에서 우연히 같아 값으로 못 가리는 축은
잡지 못한다 — 그런 축에도 그럴듯한 이름이 붙고 모든 지표는 녹색으로 남는다.

그 자리를 메우는 것이 **③ 라벨 검토**다. **절차 전문은 [`review/`](review/) 에 있다.**

## 왜 파이썬 밖인가

`src/` 는 결정적 파이썬이다. 그 안에서 LLM을 부르려면 API 키가 필요하고 특정 업체에 묶인다.
그래서 파이프라인은 **판단 직전까지** 간다 — 증거를 모으고, 미결 항목을 추리고, 거기서 멈춘다.
판단은 밖에서 아무 LLM으로나(또는 사람이) 돌리고, 결과는 언제나 같은 형식으로 돌아온다.

```
트레이스 → 라벨링 → 게이트(verify_all.py)          ← 규칙과의 일관성. 자동
                          ↓
        리뷰 패킷 full/review.md                   ← 자동
        조사 안건 research_agenda.md               ← 자동 (미해결 축 + 볼 소스 URL)
        소스 캐시 develop/sources/*.py             ← 자동 (실제 modeling·configuration)
        소스 대조 source_check.md                  ← 자동 (기계적으로 결정 가능한 것)
        검토 의뢰서 review_request.md              ← 자동 (판단이 필요한 것만)
                          ↓
        ③ 라벨 검토  review/prompt.md 를 LLM 에    ← 여기만 사람이 넘긴다
                          ↓
        review_findings.md + 원장                  ← 게이트가 만료를 감시
                          ↓
        규칙 승격 → 다음 실행부터 자동으로 잡힘
```

파이썬이 하는 일은 **증거를 다 모아놓고 판단만 안 하는 것**이다. 검토자가 준비할 것은 없다.

## 무엇이 자동으로 확인되는가

`src/source_check.py` 가 매 재생성마다 이 모델의 실제 `modeling_*.py` / `configuration_*.py` 를
받아 `develop/sources/` 에 캐시하고, 기계적으로 결정 가능한 세 가지를 대조한다 — 심볼이 읽은
config 필드의 실재 여부, `[..., X, X]` 정사각 축이 소스의 정사각 reshape 과 같은 config 필드로
이어지는지, 각 모듈 클래스가 읽는 config 속성. **확인된 것은 의뢰서에 오르지 않는다.**

소스를 못 받으면 "미확보"로 남는다 — **검사를 통과한 것이 아니라 수행되지 않은 것이다.**

## 수행 여부는 어떻게 아는가

```bash
python src/review_ledger.py     # 최신 / 만료 / 미수행
```

원장은 검토 대상 산출물의 해시를 함께 남긴다. 산출물이 바뀌면 그 검토는 자동으로 **만료**가
되고 게이트가 매번 보고한다. **안 한 것과 깨끗한 것은 구별된다.**
